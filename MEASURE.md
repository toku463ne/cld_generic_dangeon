# MEASURE.md

**この文書は測定の形式を定める。毎回この形で報告する。** 測定の結果そのものは `docs/experiments/YYYYMMDD-段階名.md` に置く（`docs/history/` には混ぜない——設計判断の記録と実験の生データは分けて、`docs/history/` からリンクする）。

**新しい指標を足したら、この文書の「用語集」に1行足すまでがセット。** 用語集に無い語をレポートに書かない。

---

## レポートの形式

測定1回につき、以下を**この順で**書く。表の列は固定し、毎回同じものを埋める。

### 1. 概要

3行以内。**何を確かめたかったか / 何が分かったか / 次に何をするか**。

### 2. 条件

| 項目 | 値 |
| --- | --- |
| コミット | （測定に使ったバイナリのハッシュ。作業ツリーが汚れていれば `-dirty`） |
| マップ | （ファイル名または名前） |
| tick 数 | |
| シード数 | （最初のシードも書く） |
| 比較対象（base） | |
| 代表シード・定常化 tick | （UI での再生に使う。下記「UI での再生」を参照） |
| 詳細レポート | `docs/experiments/YYYYMMDD-段階名.md` |

**再現方法**: 実際に叩いたコマンドをそのまま1行で書く。

```
（例）go run ./cmd/experiment -variants base,本件 -seeds 24 -seed0 1 -ticks 200000 -map typical
```

- **コミットを必ず書く。** 書かないと、後からその数字がどのコードのものか分からなくなる。**コミットしていない状態で測った数字は記録に残さない**（残すなら `-dirty` と明記し、暫定として扱う）。
- **比較対象を必ず持つ。** 単独の数字は大きいか小さいか読めない。
- **base と本件は同じバイナリで走らせる。** 別々のコミットで測った数字を並べない。

### UI での再生

実験はヘッドレスで走らせる。**実験が終わったあと、その中の1本をクライアントで見られるようにする。** 決定性があるので、状態を保存しておく必要はない——**同じ種で tick 0 から計算し直し、定常化 tick まではヘッドレスで進め、そこから先だけ描画に切り替える。**

- **代表シード**: 個体数が実験を通じての平均に一番近いシードを選ぶ。極端に崩壊した・繁栄したシードを見せても典型例にならない。
- **定常化 tick**: 個体数が実験を通じての平均から標準偏差の範囲内に初めて入った tick。それより前は立ち上がりの過渡期で、典型的な挙動ではない。
- レポートにはこの2つの値を記録するだけでよい。再生そのものはクライアント側の機能で、`cmd/experiment` は値を出すところまで。

### 3. 人口推移

| tick | 人口（全体） | 人間 | 獣 | 鳥 | 魚 | 生存シード数 |
| --- | --- | --- | --- | --- | --- | --- |

- **種族ごとに分けて出す。** 混ぜると、片方だけが増えた／減ったことが見えない。
- **平均は生存しているシードだけで取る。** 個体数ゼロになったシードを平均に混ぜると水準が見かけ上下がり、「何体で安定しているか」が読めなくなる。**生存シード数の列を必ず添える**——同じ平均でも、24本中2本の平均と24本中24本の平均では意味が違う。
- 崩壊したシード数と、初めて下限を割った tick も添える。
- **シードごとの早期終了は「崩壊の下限を割ったら、そのシードの計算を打ち切る」こと。** 実験全体の tick 数（レポートの横軸）を短くすることではない——まだ生きているシードがある限り、指定した tick 数まで観察を続ける（「短い実行では世界の運命が見えない」を参照）。全シードが下限を割った時点で、それ以降の行は省略してよい。

### 4. 出来事の回数

全て **1シードあたりの平均 ± 標準誤差**。対象期間は実行全体。

| 出来事 | 回数 |
| --- | --- |
| 交配 | |
| 出生 | |
| 種族内の戦い | |
| 種族間の戦い | |
| コインを拾った | |
| 売買 | |
| 体力消耗（合計） | |
| 死亡 | |

### 5. 行動の選択割合

決定のうち、各行動が選ばれた割合（%）。**規則が1度も発火していないことを最初に潰すための表。**

| 行動 | 割合 |
| --- | --- |

### 6. 記憶

| 種別 | 上位10 |
| --- | --- |
| 合成されていない高頻度記憶 | |
| 合成された高頻度記憶 | |
| ノード平均が高得点の中間項 | |

中間項は**値打ちの平均と、それを持っている個体の割合**を併記する。

### 7. スキル別伝承数

| スキル | 伝承された回数 | 保有率 |
| --- | --- | --- |

### 8. 死因別死亡数

| 死因 | 回数 | 割合 |
| --- | --- | --- |

### 9. 対の差（比較対象がある場合）

**同一シードどうしで引き算した平均 ± 標準誤差。** シード間のばらつきは条件差よりずっと大きいので、対にしないと埋もれる。

| 指標 | base | 本件 | 差 |
| --- | --- | --- | --- |

---

## 「強くなったか」の測り方

**世界の中の数字では測れない。** 個体数は食料で頭打ちになり、予算の平均は選択が無くても世代とともに上がり（継承するため）、死亡率は相手の強さと一緒に動く。すべて環境と共進化しているので交絡する。

### 凍結した参照ステージ

固定の試験台を1つ用意し、そこへ集団を放り込んで測る。

- 固定の地図・固定の敵・固定シード
- **試験台の中では誰も進化しない**（出生と選抜を止める、または結果に反映しない）
- 測るのは **生存日数・成人到達率・生涯出生数**

**tick T の集団と tick 0 の集団を、同じ試験台にかけて比べる。** これなら「強くなった」が交渉の余地なく出る。

**地域どうしの比較も同じ形でできる。** 地域Aの集団と地域Bの集団を同じ中立マップに入れれば、相対的な強さが直接出る。

### 安い代用

**成人到達率**（生まれた子のうち成人まで育った割合）。個体数と違って食料の上限に張り付かず、死亡率より解釈しやすい。**ただし環境が易しくなっただけでも上がる**ので、試験台の代わりにはならない。

---

## 測定の作法

- **ルールの切替は分岐ではなく設定の書き換えで表現する。** 両方の条件が同じバイナリに入っていないと、同じ種で対にして比べられない。
- **比較は必ず同一シードの対で行う。**
- **代価を共有する対照を作り、それ自体を基準にする。** 「枠は買うが効果は届かない」対照を共通の base の下に並べると、予算が動かす量のほうが大きいので、効いていても「区別がつかない」に見える。
- **個体数に触れる変更は、長い地平で「崩壊した本数」を読む。** 短い実行では世界の運命が見えない。
- **測定の前に予想を書く。** 反証可能な形で。「良くなるはず」は測定後にどうとでも解釈できる。
- **予想が外れたときほど記録に残す。** 当たった予想より学びが大きい。
- **測る側の目盛り**（連結距離など）**は設定に入れない。** 世界のルールではないため。距離が違う測定値どうしは比べられない。
- **挙動の指紋テストを持つ。** 「速くするだけの変更」が世界を変えていないことの唯一の証拠。ルールを意図して変えたときだけ、その変更と同じコミットで数値を更新する。
- **種族ごとに測る列を最初から分ける。**
- **「死ではない消え方」を作った規則は、測定器の側でも死と区別する。**

---

## 用語集

**レポートに出る語は必ずここに定義がある。** 新しい指標を足したら1行足す。

| 語 | 意味 |
| --- | --- |
| tick | 世界の最小時間単位 |
| シード | 乱数の種。同じ種なら同じ結果になる |
| コミット | 測定に使ったバイナリの git ハッシュ |
| `-dirty` | コミットしていない変更を含む状態で測ったことを示す印。暫定値として扱う |
| 標準誤差 | シード間のばらつきから出した、平均の不確かさ |
| 対の差 | 同一シードどうしで引き算した差の平均 |
| base | 比較の基準にする条件 |
| 崩壊 | 個体数が下限を割り、戻らなかった実行 |
| 代表シード | 実験の中で、個体数が実験を通じての平均に一番近いシード。UI での再生に使う |
| 定常化 tick | 個体数が実験を通じての平均から標準偏差の範囲内に初めて入った tick。UI での再生の開始点にする |
| 早期終了 | シードの個体数が崩壊の下限を割った時点で、そのシードの計算を打ち切ること。実験全体の tick 数を縮めることではない |
| 生存シード数 | ある tick の時点で、まだ個体数が崩壊の下限を割っていないシードの本数 |
| 粗い記憶 | 種別・状況・地面を主語にする記憶。伝承される |
| 細かい記憶 | 個体を主語にする記憶。伝承されない |
| 中間項 | 合成された記憶の途中にある物や出来事（コインなど） |
| 成人到達率 | 生まれた子のうち、成人まで育った割合 |
| 参照ステージ | 強さを測るための、誰も進化しない固定の試験台 |