# 2026-09-25

## 自律で進む前に、クライアントで中間を見られるようにした

**ユーザの指示**: 段階ごとの承認をやめ、`VISION.md` をなるべく実現するよう自律で進む（`CLAUDE.md`「進め方」）。その前に、動いている世界を見られるようにする。自律で進むと、6000 tick 周期の誤読（実は 300 tick の周期を 2000 tick ごとの表で読んだ見かけ）のような勘違いに気づくのが遅れるため。

**何をしたか**

- 見るだけのクライアント（`cmd/client`）は 1-2 の前から有った（`MEASURE.md`「UI での再生」）。**足りなかったのは、これまでの勘違いを目で見つけるための道具**なので、それを足した。
  - **履歴の帯**: tick 0 からの個体数と地面の食料。**1ピクセルに入る tick の最小と最大を描く**ので、1ピクセルより速い周期は帯になり、遅い偽の波にはならない（6000 tick の誤読の形）。
  - **軌跡**: 直近 300 tick の位置。閉じ込め・局所の循環・縁を回り続けるといった、1-2q で数えて初めて分かった捕まり方は、軌跡なら一目で見える。
  - **1体を追う**: クリックか `f`（体力が一番少ない個体）で追い、視界・向き・直前の決定の手と死ぬ確率・どの続きで読んだかを出す。意思決定トレース（`NODE.md`「予測の唯一の実装」）をそのまま画面に出す形。
- **描画を `view` パッケージに移し、`cmd/shot` を足した。** 同じ絵を PNG に書く。ebiten は import しただけで GLFW を初期化し、画面が無いと panic する（`DISPLAY` を外して確かめた）。1つのバイナリに `-shot` を足す形は採れなかった。`cmd/shot` は、**自律で進むあいだに Claude 自身が世界を見るための道具**でもある（PNG は読める）。あわせて、描画のテストが画面の無いところで走るようになった。
- 描画は標準ライブラリの `image` だけで行い、窓はそれを毎フレーム ebiten の画像へ書き写す。窓と PNG が同じ絵になることを優先した。ebiten の `vector` で描き続ける形は、PNG 側に2つ目の描画を書くことになるので採らなかった。
- 追っている個体の決定は `World.SetTrace` で写す。トレースは乱数を引かず世界に書かない。**追う・軌跡・描画をしても世界の指紋が変わらない**ことをテストにした（`view/view_test.go`）。

**やらなかったこと**

- WASM 出力（`CLAUDE.md`「アーキテクチャ」の最終形）。`PLAN.md` 1-4 は当面ネイティブのクライアントとしている。WSLg で窓は開けるので、見る目的には今は要らない。
- 主人公の操作（1-4 の残り）。世界のルールに触れる口が要るので、1-4 のまま。

**見て気づいたこと（まだ数えていない。次の調べものの材料）**

- 制約の地図（シード 2、tick 20000）で、**体力が一番少ない個体でも 80**。地面の食料は右岸（地域 1・3）に多く残り、身体は左の地域 0・2 に寄っている。人口の減りは、全員が飢えかけている形ではなく、ときどきまとめて死ぬ形に見える。
- 地面の食料は、ゆっくり増えて一気に減るのこぎり形（1-2q-cycle のそろい）。履歴の帯でそのまま見える。

## `VISION.md` の異議の一部が決まった（`VISION_CHECKS.md` を見直した）

ユーザが `VISION.md` を更新し、`VISION_CHECKS.md`（提案）の異議のうち次が `VISION.md` の側で決まった。表から消した。

- **O2**: 頭の良さ（記憶の並べ替えの誤差）と理性（観測の精度）を別の遺伝子にした。`NODE.md`「能力の役割を重ねない」と一致する。ただし予算の枠の行に理性が無いので、残りを O2 として問い直した。
- **O4**: 「同種のノードを食べない」は「食べれない」になった。行動の可否として実装する。
- **O5**: 性格の遺伝子は予測の事前分布を動かす、と `VISION.md` に入った。
- **O6**: 「夜は寝る」は、睡眠が必須であること（睡眠不足で能力が落ち、死ぬ）と夜の視界の制限の**結果**として書き直された。O15（予告）と O7（身体の量）に吸収した。
- **O11**: 「剣・盾の入手経路は商売しかない」は、「商売で手に入れられる状態が望ましい」「主人公はスキルを選べ、自分で作ることもできる」に変わった。スキルを選べることは O10（贈与）に合流させた。
- **O12**: 「ある程度賢い」は「ある程度合理的」になった。判定は真の表との一致率のまま。頭の良さと理性が分かれたので、(c) を役割ごとに分けて読む形にした。
- **O1** は半分決まった（点数・印象値・好感度・脅威度は保存せず読み出しにする）。同じ節の「加算・減算」「ノードに大きめの加重」との読み方を O1 として残した。

`VISION.md` に新しく入った項目（気温の範囲、水中用の食料、定住場所の変更、睡眠夜型・昼型の遺伝子、魚釣りのスキル、看板・声による売りの発信）に判定を足した。

## `VISION.md` の2回目の更新: 異議は全て決まった

- **O2**: 理性が予算の枠に入った。表から消した。
- **O1**: 印象値の加算・減算は「表示」の時に行う、と決まった。「ノードに大きめの加重」は残ったので、まず重みを置かずに作り、ノードの印象値が大きく出なければ重みを足す前に申し出る（予告）。
- **O10**: 主人公には予算外で速さの補正をかける、と `VISION.md` に入った。**`PARAMETERS.md`「プレイヤーの身体に速さを贈与しない」を書き換えた**（`PLAN.md` 1-5 の同じ線も）。補正は下限だけ・予算の外・遺伝しない、とした。分布の設計の目標（中央値の身体が補正なしで快適）は残した。補正が常に効くなら、それは分布が遅すぎるということで、下限は後付けの手当てとしてだけ働くべきだから。
- これで `VISION_CHECKS.md` の異議は全て予告になった（ユーザの決定を待つものは無い）。
- **`VISION_CHECKS.md` をユーザが承認した。** コミットの方針を「自律で進む間は Claude が自分でコミットする」に変えた（`CLAUDE.md`「コミットの方針」）。

## 1-2q: 死亡の多くは直線の往復で終わる

レポート: [`docs/experiments/20260925-1-2q-shuttle.md`](../experiments/20260925-1-2q-shuttle.md)

- **自律で進む最初の調べもの。クライアントで見て見つけた。** 制約の地図（シード 2）で、飢えかけた身体が食料の無い帯の中の1本の列を往復していた。**軸に沿った向きは跳ね返すとちょうど逆向きになり、向きを保つ身体は同じ列を往復し続ける。** 1-2q で見つけた「縁を回り続ける」「境目に捕まる」と同じ形の、局所の循環の3つ目。
- 数えると、**平坦な地図の死亡の 47.7%、制約の地図の 26.5% が直線の往復で終わっていた**（生きている身体で往復しているのは 3〜4%）。平坦な地図のゆっくりした減少の約半分は、これで説明がつく。
- **次の段階（1-2r）で、跳ね返しがちょうど逆向きになるときは斜めへ曲がる規則を足す。** 1-2q の「跳ね返し」を直す規則なので、1-2 の中の1段階として扱う。
