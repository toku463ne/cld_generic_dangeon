# 2026-09-26

## 1-2e: 実装（意図と、決め直すきっかけ）

- `Config.Recheck`（既定 300）を足し、身体は決めた手を**意図**として持つ（`engine/intent.go`）。決め直すのは、足元の食料の有無・見えている食料の集合・次の一歩が陸の外か別の地域・食料への歩きが縮まない・`Recheck` tick 経過、のどれかが真になった tick だけ。決め直しは `decide` の1か所で、値付けも同点の破り方も 1-2r のまま。`Recheck` 0 は 1-2r と同じ世界で、指紋が一致する（条件 `everytick`）。`random`・`blind`・`restless`・`straightback` の各条件も `Recheck` 0 にし、それぞれの段階の指紋のまま。
- **「食料へ歩く」意図の定義を途中で直した。** 最初は「選んだ手の死ぬ確率を、見えている食料へ歩く続きで読んだ」なら食料へ歩く意図とした。制約の地図では決定の 22% が「道が縮まない」で決め直しになり、その 97% が同じ手を選び直していた。**食料の多い地域では全部の手の死ぬ確率が 0 に丸まり、向きを保つ同点の破り方が食料から離れる手を選ぶ**——死ぬ確率は食料へ歩く続きで読んでいても、身体は食料へ向かっていない。そこで、**選んだ一歩がその食料への歩き（`walkTicks`）を 1 tick 縮めるときだけ**食料へ歩く意図とし、そうでなければ向きを保って歩く意図とした。これで決定の割合は 1 シードの試しで平坦 0.055・制約 0.11 になった。
- 「道が縮まない」の判定も、最初の「オクタイル距離が1歩ぶん（`Speed`）縮まない」から、**値付けと同じ目盛り（`walkTicks`、切り上げの tick 数）で 1 tick 縮まない**に直した。前者は値付けより細かく、値付けが同点と見る手を「縮まない」と数えていた。
- 採らなかった形: きっかけを「死ぬ確率の順位が変わりうるか」で判定する（値付けを毎 tick 回すことになり、減らしたいものそのもの）。

## 1-2e: 1回目の測定で平坦な地図の人口が下がり、見え方を身体から見た位置に直す

レポート: [`docs/experiments/20260926-1-2e.md`](../experiments/20260926-1-2e.md)

- 3faa168 で測ると、平坦な地図の 40000 tick の人口が `everytick` との対の差で **−3.17 ± 0.49（有意に低い）**。制約の地図は −6.67 ± 4.39（有意でない）。完了条件を満たさない。
- 1つ目の疑い: **待つが意図として残る。** 待っている身体は何も見え方が変わらないので、`Recheck` まで立ち止まる（待つの割合が 0.00% → 0.12〜0.20%）。`PLAN.md` の意図は3つ（食べる・食料へ歩く・向きを保つ）で待つを含まないので、**待つは 1 tick だけにして決め直す**（きっかけ `waited`）に直した。人口の差は −2.83 ± 0.66 で、原因ではなかった。
- 2つ目: 使い捨てのコードで、意図に従っている tick のうち、その場で値付けしたら意図の手が最善の手の中に無い割合を数えた（3シード、tick 5000〜20000）。平坦な地図で全体 0.39%、**死んだ身体の最後の 1000 tick では 0.82%（2倍）**。その 97% は「今なら見えている食料へ歩くほうが良い」。**食料が視界に入った瞬間は、食料の多い地図では全部の手の死ぬ確率が 0 に丸まって同点になり、向きを保って通り過ぎる。数 tick 後に体力が減ると食料へ歩くほうが良くなるが、見えている食料の集合は変わらないので決め直さない。**
- 直し方: **見え方を、地図の上の位置ではなく身体から見た位置（自分のタイルからのずれ）で持つ。** 食料が見えている間はタイルをまたぐたびに見え方が変わって決め直し、何も見えていなければまたいでも変わらない。知覚を身体中心にするのは知覚の定義として自然で、体力の帯のような閾値を足さずに済む。直したあとの差は平坦 −0.17 ± 0.59・制約 +0.92 ± 4.42（どちらも有意でない）で、決定の割合は 0.054 → 0.062（平坦）・0.125 → 0.142（制約）。
- 採らなかった形: (1) 体力の帯でも決め直す（閾値。`PLAN.md` の壊してはいけない線）。(2) 死ぬ確率を対数で持って同点を減らす（値付けそのものを変えるので 1-2r の世界が変わる。1段階1規則に反する）。(3) `Recheck` を短くする（食料が視界を通り過ぎるのは 12 tick ほどで、それより短い間隔は毎 tick 決めるのとほとんど変わらない）。

## 1-2e: 完了条件を満たし、1-2e を閉じる

レポート: [`docs/experiments/20260926-1-2e.md`](../experiments/20260926-1-2e.md)

- 8778b88 で、両方の地図で `VISION_CHECKS.md`「判断はイベント単位」の3つの判定を満たした（決定の割合 平坦 0.062・制約 0.142、人口の対の差は有意でない、きっかけがトレースに出る）。状態を「満」にした。
- **`VISION_CHECKS.md` の異議 O3 を解決として表から消した。** O3 の行: 「判断のタイミングはイベント単位 ／ 今は全身体が毎 tick 決めている ／ 1-2 が片付いたあと、1-3 の前に1段階1規則で入れる。『何も起きない』の間隔は新しいパラメータとして受け入れる。トリガーは『考え直せ』であって『こうしろ』ではない」。そのとおりに入れた（`Recheck`）。
- 予想のうち2つが外れた: 決定の割合がわずかに上（見え方を身体から見た位置にした分）、`BenchmarkWorldStep` の速くなり方が 1.24 倍（テスト用の地図は決定が密）。実験の地図では 1シードあたり 2.2〜2.7 倍速い。
- `PLAN.md` の 1-2e の節を下に移し、`PLAN.md` には結論だけ残した。1-3 の base は 1-2e。

### `PLAN.md` から移した節

#### 1-2e イベント単位の判断（`VISION_CHECKS.md` O3）

**何を作るか**
身体は決めた手を**意図**として持ち（食べる／見えている食料へ歩く／向きを保って歩く）、**次のどれかが起きた tick にだけ決め直す**。それ以外の tick は意図に従って1歩進むだけで、値付けをしない。

1. 足元の食料が変わった（「食べる」が候補に入る・外れる）
2. 見えている食料が変わった
3. 今の向きの次の一歩が、陸の外・水・今と別の地域に出る
4. 食料へ歩いているとき、今の向きがもう道を縮めない（食料と縦か横に並んだ・着いた）
5. `Recheck` tick のあいだ何も起きなかった（`VISION.md` の「何 tick か何も起こらなかった」）

`Config.Recheck`（既定 300 ＝ 1食で戻る体力を燃やす tick 数）。0 なら毎 tick 決める（1-2r の世界。条件 `everytick`）。決め直す手順（値付け・向きを保つ・跳ね返し）は今のまま変えない——変わるのは**いつ**決めるかだけ。

**実装前に数えたこと**: [`docs/experiments/20260925-1-2e-change.md`](docs/experiments/20260925-1-2e-change.md)。決定の 98% は前と同じ手。手の変わり目の 83〜84% は 1〜3 の直後で、4 を足すと約 99.6%（平坦）・94.6%（制約）。捉えられない残り（制約で約 5%）は、地域の食料の数が他の身体に食べられて変わり、値付けの順位が入れ替わったもの——身体からは見えない変化なので、見逃すのは `VISION.md` の意図どおり。同じ手が続く長さは最大 271 tick なので、`Recheck` 300 はほぼ効かない（安全網）。

**予想**（測る前に書く）
- 決定の回数 ÷（生きている身体 × tick）: 平坦 0.06 以下、制約 0.12 以下。
- 40000 tick の人口は `everytick` との対の差で有意でない（両方の地図）。崩壊 0 / 12。
- 指紋は変わる（乱数を引く回数が減る）。
- `BenchmarkWorldStep` は 2 倍以上速くなる。

**完了条件**: `VISION_CHECKS.md`「判断はイベント単位」の判定（決定の回数の割合が 1 より有意に小さい、トリガーの種類がトレースに出る、`everytick` との対の差で人口が有意に下がらない）を両方の地図で満たす。

**壊してはいけない線**
- トリガーは「考え直せ」であって「こうしろ」ではない（`NODE.md`）。トリガーの中で行動を決めない。
- ハードコードされた行動閾値を作らない。体力の帯をトリガーにしない（数え上げで、体力だけで手が変わる決定は 0.1% 未満だった）。
- 予測の唯一の実装。意図に従う tick も、決め直す tick も、値付けは `decide` の1か所。

---


## 1-3: 数え上げ（交配の機会はある、生命を先に比べると誰も交配しない、大人は死なない）

レポート: [`docs/experiments/20260926-1-3-breed.md`](../experiments/20260926-1-3-breed.md)

- 視界に他の身体がいる決定は約 34%（両方の地図）。交配の機会は実在する。
- 子のために体力を払うと、「動き続ける」の死ぬ確率がちょうど変わらない決定は 0%。**生命を先に比べ、子は同点のときだけ数える（辞書式）読み方では、交配は起きない**（起きるとしたら浮動小数点の 0 への丸めだけ）。ユーザに3案を示し、**「手の点数 ＝ 死ぬ確率 − ChildWorth × 子ができる確率、ChildWorth < 1」**に決まった（既定 0.5 ＝ 親子の近縁度。確実な子は確実な命より安い、が「生命 > 子孫」の読み方）。採らなかった形: (1) 辞書式で出生の体力をただにする（交配が比較ではなく常に選ばれ、体力が無から生まれる）。(2) 辞書式で体力を払う（上のとおり起きない）。
- 死ぬ道が餓死だけのとき、最初の身体の 74%・42% が 40000 tick 後も生きている。**`VISION_CHECKS.md` O8 の予告どおり、老いによる死を 1-3 の直後に1段階として入れる。**
- 交配を相手を名指しする手にし、出生は両者の意図が互いを指したときだけ起こす（`VISION_CHECKS.md`「交配は両者の合意」の構造のテスト）。相手を探して歩く行は入れない（1段階1規則。視界の中にいる相手とだけ交配する）。

## 1-3: 実装（交配・出生・2通貨の点数）

- `engine/breed.go`。交配は視界の中の成人を名指しする手（`ActMate`）で、払う分（`BirthEnergy` の半分）を払っても体力が残るときだけ候補に出る。出生は、交配の手を実行したときに相手の今の意図が自分との交配であるときだけ（合意）。両親が半分ずつ払い、子は親の位置に `BirthEnergy` で生まれ、`MatureAge` tick 後に成人する。生まれたあとは両者の意図を「待つ」にして、同じ tick の相手の番で2体目が生まれないようにした。
- 手の点数 ＝ 死ぬ確率 − `ChildWorth` × 子ができる確率（交配は 1、それ以外は 0）。`decide` は点数で最小と同点を決める。トレースに `Child`・`Score` を足した（予測の唯一の実装）。
- 決め直すきっかけに `partner`（視界の中の成人の顔ぶれ。ID の混ぜ合わせの和で、見つかる順に依らない）を足した。交配は待つと同じく意図にならない。**出生は相手の意図を読むので、`Recheck` 0 でも意図は毎回記録する**（指紋に入れるのは `Recheck` が正のときだけなので、`everytick` の指紋は変わらない）。
- 視界の中の成人を探すため、タイルごとの身体の一覧（`grid`）を持つ。tick の終わり（死者が抜け、子が加わったあと）に作り直し、移動のたびに更新する。身体から作り直せるので保存も指紋もしない。
- 子は親2体の ID（`Parents`）を持つ。合意の構造のテスト（親の出生前の最後の決定が互いの名指し）に使い、あとで血統タグの材料になる。
- **クライアントで見て分かったこと**: 死ぬ確率がすでに 0.97 の身体が交配する（払っても 0.99 にしか上がらず、上がり幅が `ChildWorth` より小さい）。死にかけの身体ほど子に賭けるのは、死ぬ確率で値付けする形の帰結。また、相手が応じないと、申し出た身体はその場で申し出を繰り返す（表は相手の合意を 1 と読む。他の身体に先に食べられることを表に入れていないのと同じ省略）。申し出 1 回あたりの出生を測定で数える。
- 採らなかった形: (1) 相手を探して歩く行（1段階1規則。交配の機会は視界の中で約 34%）。(2) 交配のあとの休止期間（規則が1つ増える。払う体力が同じ役を果たすかを先に見る）。(3) 子ができる確率を相手の意図で割り引く（最初に申し出る側は常に 0 になり、誰も始めない）。

## 1-3: 完了条件を満たし、1-3 を閉じる

レポート: [`docs/experiments/20260926-1-3.md`](../experiments/20260926-1-3.md)

- 1888d9d で、両方の地図で完了条件を満たした: 出生 約 6000 体／シード、崩壊 0 / 12（下限 20）、交配は比較の結果として選ばれていることがトレースで読める、占有地域数を記録できる。人口は交配なしより平坦 +22.8・制約 +88.5 で、両方とも約 170 体で横ばい。
- **予想が外れた: 交配を見送るのは空腹の身体ではなかった。** 交配の手が候補にあれば 99.99% 選ばれ、見送った決定の体力は選んだ決定より 12〜20 高い。死ぬ確率は体力に対して S 字で、**死にかけの身体は払っても死ぬ確率がほとんど上がらない**。選んだ／見送ったは、払うことによる上がり幅が `ChildWorth` の上か下かでちょうど分かれる。閾値を書かずに比較から出た形だが、「生命 > 子孫」を「死にかけても命を優先する」と読むなら、この形はそう振る舞わない——**点数の形（ユーザ決定）の帰結として記録し、ユーザに報告する。**
- **世代は回る。** 1-2e の数え上げでは満腹の大人がいつまでも生きたが、1-3 では子のために払う体力で大人が死に、最初の身体は1体も tick 40000 まで生きない（死んだ年齢の中央値 1201、最大 約 4000）。**`VISION_CHECKS.md` O8 の予告（老いによる死を 1-3 の直後に入れる）は、前提が消えたので成長・老い・性別の段階へ回した。** O8 の行と「老人の後は死亡」の測れる段階を書き換えた（判定の中身は変えていない）。
- 崩壊の下限を 20（仮）にした: 崩壊しなかった実行の個体数の谷の最小は 119（平坦 122）。谷はどちらも最初の身体が一斉に空腹になる tick 2000 前後。
- 定常化 tick が 0 になった（人口が tick ごとに大きく揺れ、標準偏差が大きいため）。`MEASURE.md` の定義どおりなので直さず、レポートに注記した。
- `VISION_CHECKS.md`: 「簡単に絶滅しない」「交配は両者の合意」を満、「交配で子孫を残す」の出生の部分を満にした。
- クライアントで見て分かったこと: 交配なしでは食料が余って身体は白く、1-3 では食料が食べ尽くされて黒い身体が多い（人口が食料の上限に張り付く）。食料の 300 tick の周期は消える。

### `PLAN.md` から移した節

#### 1-3 繁殖（子の通貨が働く・身体は全員同じ）

**何を作るか**
交配と出生を作る。子の身体は全員同じで、予算はまだ何も買わない。子は成人するまで交配を行えない（行動の可否として表す）。これで2つ目の通貨「子ができる確率の変化」が非ゼロになる。

- **交配は相手を名指しする手**（`ActMate`）。候補に出るのは、自分が成人で、視界（`Sight`）の中に成人の身体がいて、自分の払う分（`BirthEnergy` の半分）を払っても体力が残るときだけ（払うと死ぬのは「身体にできないこと」）。相手の体力は見えない（隠しパラメータ）。
- **出生は両者の合意でだけ起きる。** 交配の手を実行した tick に、相手の今の意図が「自分との交配」であり、相手がまだ視界の中にいて、両方が払えるときに限り、両親が半分ずつ払って子が1体生まれる（体力 `BirthEnergy`、成人は `MatureAge` tick 後）。片方だけが選んだ交配は何も起こさない（その tick は待つのと同じ）。
- **値付けは2通貨を1つの比較に入れる**（ユーザ決定、2026-09-26）: 手の点数 ＝ 窓の終わりに死んでいる確率 − `ChildWorth` ×（その手で子ができる確率）。一番小さい手を取る。`ChildWorth` は 1 未満（生命 > 子孫: 確実な子は確実な命より安い）で、既定 0.5（親子の近縁度）。交配の手の子ができる確率は 1（合意すれば必ず生まれる。繁殖能力で確率が変わるのは予算の段階）、それ以外の手は 0（相手を探して歩く行は、まだ表に無い）。
- **決め直すきっかけに `partner`（視界の中の成人の顔ぶれが変わった）を足す。** 交配は待つと同じく意図にならない（1 tick で決め直す）。
- 設定: `Breed`（false で 1-2e の世界。条件 `nobreed`）・`ChildWorth`・`BirthEnergy`・`MatureAge`。

**実装前に数えたこと**: [`docs/experiments/20260926-1-3-breed.md`](docs/experiments/20260926-1-3-breed.md)
- **子の通貨が非ゼロになる決定の割合**: 視界（3×3）に他の身体がいる決定は約 34%（両方の地図）、同じタイルは約 5%。交配の機会は実在する。
- **体力を払うと死ぬ確率は必ず上がる**（ちょうど変わらない決定は 0%）。生命を先に比べて子は同点のときだけ、という読み方では誰も交配しない——これが `ChildWorth` の形を決めた。
- **世代は回らない**: 死ぬ道が餓死だけの世界では、最初の身体の 74%（平坦）・42%（制約）が 40000 tick 後も生きている。死ぬ身体は立ち上がりに集中する（中央値 3101・2201 tick）。**満腹の大人はいつまでも生きる**——`VISION_CHECKS.md` O8 のとおり、老いによる死を 1-3 の直後に1段階として入れる。
- **成人までの期間** `MatureAge` ＝ 1000（満腹から餓死までの tick 数。成人するには一度は自分で食べる必要がある長さ）。死んだ身体の死んだ年齢の 10% 点（1001・2201）とほぼ同じか短いので、成人到達率は測れる。
- **子の体力** `BirthEnergy` ＝ 50（満腹の半分＝500 tick。食事の周期 300 tick より長い）。
- **崩壊の下限**は、この段階の実験で崩壊しなかった実行の個体数の谷を数えてから決める（下の完了条件）。

**予想**（測る前に書く）
- 両方の地図で、個体数は食料の上限に張り付く（1-2 より上がり、そこで止まる）: 40000 tick の人口が `nobreed`（1-2e）との対の差で有意に多く、最後の 4 分の 1 で傾きが有意に負でない。
- 出生は1シードあたり 100 体以上（両方の地図）。餓死も `nobreed` より有意に多い（増えた分は食料の上限で死ぬ）。
- **成人到達率は、制約の地図のほうが平坦な地図より低い。**
- 交配を選んだ決定の直前の体力は、交配の手が候補にあったのに選ばなかった決定より有意に高い（閾値ではなく、払う体力の値付けで空腹の身体が見送る）。
- 決定の割合は 1-2e より上がるが 0.3 未満（`partner` のきっかけが増える）。
- 制約の地図の占有地域数は、ほぼ全 tick で 4（最初の配置で両岸にいて、どの地域にも食料がある）。
- **身体は陸でつながった地域すべてに広がり、水で隔てられた地域には入らない。** 広がりを抑える規則はまだ無いので、これは予想どおりに起きるべき問題として測る（`NODE.md`「普通の身体は自分では広がらない」）。

**完了条件**
- 両方の地図で、交配・出生が起き（§4）、崩壊しない。
- §5 で、交配が選ばれていて、閾値ではなく比較の結果として選ばれていることがトレースで読める。
- 占有した地域の数を時系列で記録できる（用語集に「占有地域数」を足す）。
- `cmd/experiment` に、崩壊の下限の仮の既定値が入る。

**壊してはいけない線**
- 目的は生命 > 子孫（`NODE.md`）。子の通貨は生存の通貨と同じ比較の中で扱い、別枠の欲求にしない。
- ハードコードされた行動閾値を作らない。「満腹なら交配する」を書かない（`NODE.md`）。
- 普通の身体は自分では広がらない（`NODE.md`）。この段階では抑えずに**測るだけ**にする。抑える規則を一緒に入れると、何が効いたか分からなくなる。

**判断に使う表**: §3、§4、§5、§8、§9（base＝1-2e）、成人到達率（`MEASURE.md`「安い代用」）。

---


## 1-3: 世代が回る理由（世代交代か、大量死からの再生か）

レポート: [`docs/experiments/20260926-1-3-turnover.md`](../experiments/20260926-1-3-turnover.md)

- ユーザの依頼で、老いが無いのに世代が回る理由を、次の段階へ進む前に切り分けた。
- **大量死からの再生ではない。** 100 tick ごとの死亡数の分散 ÷ 平均は約 1.1、人口の変動係数は 4%。**世代交代だが、老いではなく子の代金で回る**: 死亡の約 75% は直前 1000 tick 以内に子の代金を払った成人で、払ってから死ぬまでの中央値は約 105 tick。払った tick の親の体力の中央値は 45〜46。死んだ成人の年齢は 1150〜1700 tick に固まり、子の数の中央値は 2。死因は全て餓死（他の死因がまだ無い）。
- ユーザが `VISION.md` に「死にかけのノードは回復手段が見つからないとき、子孫を残すために行動することがある（確率的）」を足した。`VISION_CHECKS.md` に判定の行と、予告の異議 O16 を足した（判定はユーザの承認待ち）。今の点数は死にかけの身体をすでに交配させるが決定的で、確率的にするのは頭の良さ（並べ替えの誤差）の段階に任せる。

## 1-3: 死にかけの身体の交配（新しい判定の (a)(c)）

レポート: [`docs/experiments/20260926-1-3-dying.md`](../experiments/20260926-1-3-dying.md)

- ユーザが判定の行を承認した直後に測った。承認された「死にかけ」（交配以外の最善の手の死ぬ確率 0.5 以上）は、見えている食料へ歩く続きをすでに含むので、「食料が見える死にかけ」が食料で助からない身体だけになり、(c) の差が原理的に出なかった（両方 1.0000）。**判定の「死にかけ」を「食べなければ死にかけ」（地域を動き続ける行だけで読んだ死ぬ確率 0.5 以上）に直した。判定の中身の変更なので、ユーザに申し出る。**
- 直した判定で (a)(c) は両方の地図で満。ただし (c) の差は 0.5〜1% と小さい: 交配しても、見えている食料へはそのあと歩いて食べられるので、表の上では交配が回復手段を奪わない。(b) は満たさない（ほぼ 100%）。

## 1-4: 数え方を直し、身体ごとの能力の口を入れる

- `PLAN.md` 1-4 の「速さの相手」を、**全員一律に振るのではなく、生まれた身体の半分だけ振って同じ世界の中で比べる（侵入）**形に直した。1-3 の世界は出生と死亡が釣り合っているので、一律に振っても1体あたりの生涯の子の数は釣り合いの値に張り付き、選択勾配が出ない。段階の切り方は Claude が決めてよい範囲（`CLAUDE.md`「進め方」）。
- そのための口として、身体ごとの能力 `Build`（速さ・体力の上限・消耗。0 は設定の値）と、実験のためだけの `World.SetBuild` を入れた。規則ではない（どの規則も能力を決めない）。全員 0 なら 1-3 と指紋が一致し、設定と同じ値を書いた能力は 0 と同じ世界になる（テスト）。値付けは身体ごとの能力で読み、生存の表は地域・食料の数・能力ごとにキャッシュする。
- 性能: 能力を読むだけで `BenchmarkWorldStep` が約 6% 遅くなった（53.9 → 57.5 µs）。プロファイルで、`Map` の値のレシーバ（`InBounds` など）が呼ぶたびに `Map` を複写していたのが見つかったので、ポインタのレシーバにした。**結果は 1-3 より速い**（51.9 µs、`BenchmarkDecide` 498 → 403 ns）。

## 1-4: 数え上げ（速さの相手は体力の上限）

レポート: [`docs/experiments/20260926-1-4-invade.md`](../experiments/20260926-1-4-invade.md)

- 生まれた身体の半分の能力を振る侵入で数えた。**速さだけが上げても下げても生涯出生数を動かす。** 体力の上限・消耗・その両方は、設定の値より良くしても生涯出生数はほぼ 0、悪くすると大きく下がる。燃費を良くすると成人到達率は上がるが成人1体あたりの子の数が下がり、打ち消し合う。
- `PLAN.md` の「片側がゼロなら取り合いは起きず、配分は端に張り付く」は、取り合いの両側（速さを上げる得と相手を下げる損）がどちらも非ゼロかどうかと読んだ。どの候補も満たす。**速さの相手は (a) 体力の上限**にした: `VISION.md` の遺伝子の一覧にある（燃費は無い）。(c) は代価を2つ重ねるので、1つで足りる以上は採らない。
- 画面横断時間の見積もり: 今のクライアントは地図全体を出すので、速さ 0.25 で ×1 4.3 秒。

## 1-4: engine の外から選ぶ口と、クライアントの操作

- `engine.Chooser`（`Takes(id)`・`Choose(body, valuation)`）と `World.SetChooser`。名指しした身体は毎 tick 決め（きっかけ `outside`）、口が返した手が候補にあればそれ、無ければ値付けの手を取る。engine は誰が選んでいるかを知らない。口が無い、または誰も名指ししないときは指紋が変わらない（テスト）。**予算の配分より先に入れ、1-3 の指紋のままであることを確かめた**（`PLAN.md` 1-4 の完了条件）。
- トレースは、口が選び直したあとの手を出すように、`decide` の中から `turn` へ移した（トレースが実際に取った手と食い違わないように）。
- クライアント: `c` で追っている身体を操作し、`wasd`・`e`・`r` で選ぶ（`view.Driver`）。何も押さなければ待つ。**操作していて快適かどうかは 1-5 でユーザが確かめる。**
- 採らなかった形: 何も押さないときは値付けに任せる（操作しているのに勝手に動くと、何が自分の手か分からない）。

## 1-4: 予算の配分（速さ ⇔ 体力の上限）

- `Allot`: 生まれたとき速さへの配分 s を ½ ± 0.25 の 11 段から等しく引き（継承しない）、速さ ＝ 設定 ×（2s）^0.5、上限 ＝ 設定 ×（2(1−s)）^0.5。逓減で上を締め、天井は置かない。乱数は世界の1本から、身体1体につき1回。`fixed`（`Allot` false）が 1-3 の指紋のまま。
- **配分を連続にしなかった。** 生存の表は身体の能力ごとに作るので、連続にすると身体ごとに表を作ることになり、テストが 10 倍遅くなった。11 段にして表を身体どうしで共有する。段は 1-5 の突然変異（1段ずらす）とも相性が良い。
- 性能: 11 段でも表を段ごとに作るので、実験の地図で1シード 40000 tick が 1.6 → 4.6 秒（約 3 倍）。`BenchmarkWorldStep` はテストの小さな地図で 1000 tick ごとに世界を作り直す（表を作り直す）ので 52 → 約 390 µs。**払ったもの: 実験の時間が約 3 倍。得たもの: 身体ごとに違う能力と、その能力で読む値付け。** 段を減らす・表を食料の数について補間する（値付けが近似になる）は、実験の時間が問題になったら考える。
- 実験のレポートに「配分と生涯出生数」（配分ごとの生涯出生数、速さの側 − 上限の側の対の差、生きている身体の配分の平均）を足した。

## 1-4: 完了条件を満たし、1-4 を閉じる

レポート: [`docs/experiments/20260926-1-4.md`](../experiments/20260926-1-4.md)

- 生涯出生数は配分に対して山なりで、山は半分ずつ（0.50）。速さの側が上限の側より多い（+0.26・+0.29）。個体数は `fixed` と変わらず、出生はわずかに減った（−120／シード）。
- **予想「制約の地図では速さの側、平坦な地図では相手の側」は外れた**——両方の地図で速さの側。1-3 の世界は両方の地図で食料が食べ尽くされていて（人口がどちらも約 170）、地図の違いが身体の暮らしに届いていないと読める。**地図によって逆符号になる例ではない**（同じ符号）。
- `VISION_CHECKS.md`「ノードによって賢さや力強さなどの特徴が出る」を、速さと体力の上限について満にした。
- 次は 1-5（継承と突然変異）。速度の分布を確定させ、**中央値の身体を操作して快適かをユーザが確かめる**段階。

### `PLAN.md` から移した節

#### 1-4 予算の配分（速さ ⇔ もう1つの能力）と最小のクライアント

**何を作るか**
身体に予算を持たせ、**速さと、もう1つの能力**に配る。**どちらへの変換も逓減させる**（`PARAMETERS.md`「硬い上限ではなく逓減」）。子の配分は、まだ**固定の分布から引く**（継承しない）。あわせて **ebiten（ネイティブ）の最小のクライアント**に主人公1体の操作を足す。見るだけのクライアント（`cmd/client`。描画と再生）は、`MEASURE.md`「UI での再生」のために先に作ってある。

変換を2つ同時に入れるのは、買い先が1つだと全員が同じ配分になり、何も測れないから。ルールとしては「予算を配る」の1つと数える。**クライアントは世界のルールではないので、1段階1規則の数には入れない。** ただし、engine 側に足す「行動を差し替える口」は世界に触れるコードなので、使わないときに世界を1ビットも変えないことを完了条件で確かめる。

**実装前に数えること**
- **速さの相手を何にするか。** 候補は3つ。
  - (a) 体力の上限（飢えに耐える長さ）だけ
  - (b) 速さの燃費（速い身体ほど体力を使う）だけ
  - (c) (a) と (b) の両方

  **数え方を「全員一律に振る」から「半分だけ振る（侵入）」に直した**（2026-09-26）: 1-3 の世界は出生と死亡が釣り合っているので、全員一律に振っても1体あたりの生涯の子の数は釣り合いの値（死んだ身体1体あたり親として2回）に張り付き、選択勾配が出ない。**同じ世界の中で身体ごとに違えて、違えた身体と違えない身体を比べる。** そのための口として、身体ごとの能力（`Build`: 速さ・体力の上限・燃費。0 は設定の値）と、実験のためだけの `World.SetBuild` を先に入れる（規則ではない。全員 0 なら 1-3 と指紋が一致すること）。値付けは身体ごとの能力で読む（速い身体は自分の速さで予測する）。
  - 各候補の能力を、**生まれた身体の半分**（数え上げ側の乱数。世界の乱数は引かない）で ×1.25 と ×0.8 に振り、残り半分（設定の値）と、同じシードの中で比べる。読むのは**成人到達率**と**死んだ成人1体あたりの子の数**の差。燃費は、移動が行動の約 98% なので、1 tick あたりの消耗（`EnergyBurn`）で代える。
  - **両方の地図で、上げる側と下げる側の両方の差が有意になる候補を選ぶ。** 速さ自身も同じ形で測り、速さに勾配が無ければ 1-4 の前提から見直す。(a)・(b) とも片側がほぼゼロで (c) だけ非ゼロなら「代価1つでは足りない」として (c) を採る。3つとも片側がほぼゼロなら 1-4 は作らず、身体の代価の設計に戻る。
- **画面横断時間の見積もり。** 遊ぶズームと1フレームあたりの tick 数の仮の値で、1-3 の身体（全員同じ速さ）が画面を横切るのに何秒かかるかを計算する。この数字が 1-4 の「中央値の身体」の出発点になる。

**実装前に数えたこと**: [`docs/experiments/20260926-1-4-invade.md`](docs/experiments/20260926-1-4-invade.md)。速さは上げても下げても生涯出生数が動く（+0.6〜+0.7・−0.67）。体力の上限・消耗は、良くしても動かず悪くすると下がる。**速さの相手は (a) 体力の上限**（`VISION.md` の遺伝子の一覧にあり、下げる側の勾配が両方の地図で有意）。画面横断時間は地図全体を出す今のクライアントで ×1 4.3 秒。

**予想**
- 固定の分布から引いた配分でも、**生涯出生数は配分によって有意に違う**（選択勾配がある）。**制約の地図では速さの側、平坦な地図では相手の側に勾配が寄る**と予想する。外れたら、逆符号の例として記録する。
- （数え上げのあとに足した予想）配分と生涯出生数の関係は山なりで、山は「半分ずつ」より速さの側にある（侵入で、速さの得が上限の損とほぼ同じ大きさだったため、山は半分ずつの近くで、やや速さの側）。両方の地図で同じ向き。
- 個体数は 1-3 から動かない（食料で頭打ちのため）。

**完了条件**
- 両方の地図で、配分と生涯出生数の関係（選択勾配）を §9 と同じ対の形で出せる。
- クライアントで主人公1体を動かせる。主人公は他のノードと同じ行動の集合を使い、engine からは区別がつかない。
- 行動を差し替える口を使わない実行では、挙動の指紋テストの値が、口を足す前と一致する。予算配分を入れると指紋の値が変わるので、**口は予算配分より先に足し、その時点で 1-3 の指紋と一致することを確かめる。**
- `PARAMETERS.md` の「予算→速さの変換」に仮の値が入る。

**壊してはいけない線**
- 能力の役割を重ねない（`NODE.md`）。速さは移動にだけ効かせ、相手の能力は1つの役割だけを持つ。
- **engine は「プレイヤー」を知らない**（`CLAUDE.md`）。操作は、行動を差し替える口を engine の外から叩く形にする。
- engine は描画を知らない（`CLAUDE.md`）。クライアントが `engine` を import する向きだけ。
- 種族ごとの分岐をコードに書かない（`CLAUDE.md`「種族」）。

**判断に使う表**: §3、§4、§9（base＝1-3）、成人到達率、`PARAMETERS.md` の速度の表。

---


## 性能: 体感を目標にし、µs を代理指標にする

レポート: [`docs/experiments/20260926-speed.md`](../experiments/20260926-speed.md)

- ユーザの依頼で、前身と「同じくらいの個体数・同じ tick 数で何秒か」を測った。約 127 体・10000 tick で前身 3.00 秒・今 0.31 秒（1コア）、WASM（node）で前身 12.9 秒・今 1.07 秒。どちらのエンジンもゴルーチンを使わないので `GOMAXPROCS=1` はほとんど変わらない。
- **見つかったこと: セーブを読み込んだ直後の 1000 tick が 13〜17 倍遅い**（生存の表は保存しないので作り直す）。WASM で 1.7 ms/tick で、スマートフォンの速い設定では目標に入らない。配るスナップショットで遊ぶ段階までに直す候補（表を保存する・予め作る・段を減らす）。
- `PARAMETERS.md`「性能の上限」を、最終目標（スマートフォンのブラウザでリアルタイムに止まらず遊べる）と、それを確かめる代理指標（`cmd/speed` の秒数・WASM の秒数・読み込み直後・µs のベンチ）の形に書き換えた。前身の確認は「リアルタイム・開発機の WASM と native・実機なし・人 120〜150 体」で、今回の想定に近い。ただし今は前身より約 10 倍速いので、前身の実績は下限としては緩い。悪化しうるのは記憶（1-6 以降）・個体数・読み込み直後の3つ。
- `cmd/speed` を足した。

## 衝突: 他の身体のいるタイルへは入れない

レポート（数え上げ）: [`docs/experiments/20260926-collide.md`](../experiments/20260926-collide.md)

- ユーザの提案: 「占有タイルは移動先の候補から外す」。判断のときだけ考慮されるので判断回数は増えず、移動中に塞がっていたら跳ね返り規則が働く。外すのは次の1歩だけで、視界の食料を目的地に選ぶときは占有を理由に外さない。「混んだ食料は取れないことが多い」は規則に書かず、1-6 以降の記憶に学ばせる。
- **確かめて合っていたこと**: 空間の格子（`grid`）は交配の知覚のために毎 tick 更新されていて、タイルが塞がっているかは1タイルの一覧を見るだけ。候補から外す形なら、判断のときの跳ね返り（`bounce`）は塞がった向きを壁と同じに扱う。食料を目的地にする読み（真の表の4行目）は占有を見ないので、空腹の身体が見えている食料を無視することはない。塞がったタイルに食料があった移動は 0.9% だけ。
- **合わなかったこと**: 跳ね返りは `decide` の中でだけ働く。**タイルをまたぐ移動の 91〜94% は意図に従っている tick に起きる**ので、判断のときだけ外す形では衝突の 9 割に届かない。意図に従っていて次の1歩が塞がったとき（身体×tick の 1.5〜2.1%）の扱いを、ユーザが3案から**「その tick に決め直す」（きっかけ `blocked`）**に決めた。決め直した身体は空いたタイルを選ぶので、塞がり続けるのは四方を囲まれた身体だけ。採らなかった形: 値付けをせず跳ね返す（値付けの外の行動規則になる）、止まって意図を保つ（向かい合った2体が `Recheck` まで動かない）。
- 自分のタイルの中で動く手は、同じタイルに他の身体がいても入れる。生まれた子は親の位置に生まれ、同じタイルにいてよい。
- **条件の作り方を直した**: 1段前の段階の条件を「次の段階の条件から規則を1つ抜く」連鎖で書くようにした（`overlap` → `fixed` → `nobreed` → `everytick` → …）。書き換えの途中で、`gofmt` が桁をそろえ直したために置き換えが外れ、**1-3 より前の条件が衝突ありで走る**ところだった。連鎖なら、あとで足す規則は前の条件で必ず切れる。各条件で後の規則が切れていることをテストにした。
- `Value`（テストが使う、設定の表での値付け）は、身体の能力を設定の値として読むようにした。配分のある身体をそのまま渡すと、表の範囲の外を読んでいた（衝突を入れて表に出た。1-4 から潜んでいた）。

## 衝突: 測定

レポート: [`docs/experiments/20260926-collide-run.md`](../experiments/20260926-collide-run.md)

- 決定の割合は 0.084 → 0.099（平坦）・0.107 → 0.124（制約）で、数え上げからの見積もりどおり。跳ね上がらない。1 tick は +10%（増えた決定の分）。
- **個体数は動かなかった。** ユーザの見立て「混雑地で移動が制限され、空間も個体数の制約になる」は、今の地図ではまだ起きない（身体はタイルの 6% ほど、食料が食べ尽くされて散っている）。身体が集まる理由ができる段階（定住場所・巣・地形）で改めて測る。
- 速さの側の有利が +0.26・+0.29 から +0.15・+0.13 に縮んだ。1-5 で配分を継承させるとき、動く向きは同じだが幅は小さいはず。
