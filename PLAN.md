# PLAN.md

**この文書は、これから作るものの段階・実装前に数えること・予想・完了条件だけを書く。** 済んだ段階の節は `docs/history/YYYYMMDD.md` へ移し、ここからは消す。ルールの今の姿は `NODE.md` / `ITEMS.md` / `MAP.md`、原則は `CLAUDE.md`、報告の形は `MEASURE.md`。

---

## 読み方

- `CLAUDE.md` の「最初の順序」の3つを、**1段階につきルール1つ**に割ったもの。測ったときにどれが効いたか分かるようにするため。
- **段階1・2は、その結果で形が変わる。** 段階3以降は見出しだけにしてある。細かく書くのは、段階2の完了条件が真になってから。
- 各段階は次の6項目を持つ。
  - **何を作るか**
  - **実装前に数えること** —— そのルールが働く余地が実在するかを、ルールを入れずに測る方法。**決めていないことはここで決める**
  - **予想** —— 実装して測る前に書く。反証できる形で
  - **完了条件** —— 何が真なら次へ進むか
  - **壊してはいけない線** —— その段階で踏みやすい原則。出典の文書名つき
  - **判断に使う表** —— `MEASURE.md` の「レポートの形式」の節番号
- 「有意」は `MEASURE.md` の対の差で、**差が標準誤差の2倍を超えること**を指す。

## 段階1・2で共通の条件

- **種族は人間だけ。** 表の種族列（獣・鳥・魚）は、形式を崩さないため空欄のまま残す（`MEASURE.md`「種族ごとに測る列を最初から分ける」）。
- **水は地形。** 人間は水のタイルへ移動できない（`NODE.md`「行動として可能なこと＝移動の可否」）。溺れるという規則はまだ入れない。
- **地図は2枚。どちらもコードで作る**（Tiled は段階3以降）。**毎回両方で測り、どちらの地図で出た数字かを必ず書く**（`CLAUDE.md` の教訓「同じ規則が地図によって逆符号になる」）。
  - **平坦**: 全面が陸、食料の湧く速度は一様。校正用
  - **制約**: 地域ごとに食料の湧く速度が違い、水が陸を区切る。食料が人口を縛る
- **地図を作るコードは engine の外に置く。** engine が受け取るのはタイルの配列だけ（`MAP.md`「engine はファイルを知らない」）。
- **食料は1種類**（汎用食料）。

---

## 段階1 最小構成と速度の分布

`CLAUDE.md` の「最初の順序」の1にあたる。身体・食料・水（地形）だけの世界で「記憶は結果、値付けは2通貨」が動くことを確かめ、**同じ段階の中で速度の分布を確定させる。**

この段階は2つに分かれる。

- **1-0〜1-5**: **学習しない固定の表**（世界の真の規則から作った結果の表）で値付けを確かめる。全員が正解を知っている対照になる
- **1-6**: その表を**経験から学ぶ粗い行**に置き換える。個体を主語にした行と伝承は、まだ入れない

**速度の分布は 1-5 で確定する。** 1-2・1-3 の完了条件は「発火するか」「崩壊しないか」だけにして、数値には頼らない。それらの数値は 1-5 のあとで取り直す（`PARAMETERS.md`「後から変えると先行する測定が全部読み直しになる」）。

### 1-0・1-1（済み）

節は `docs/history/20260923.md` に移した。ここに残すのは、後の段階が前提にする結論だけ。

- **1-1 の無作為の対照は、両方の地図で全滅する。** 出会って食べる頻度が、体力の釣り合いに届かない。**世界の値は、対照が生きられるようには直さない**（対照に合わせて世界を易しくすると、1-2 で値付けが働く余地が減る）。
- そのため、**シード数と tick 数は 1-2 で、崩壊の下限は 1-3 で仮に決める。** 選別を読む基準線は、無作為の実測ではなく、地図から計算した面積比を使う。
- **崩壊の下限が決まるまでは、下限を 0（絶滅）として扱う。** 生存シード数と早期終了は、この値で数える。

---

### 1-2 値付け（生存の通貨だけ・真の規則の表）

**何を作るか**
無作為の判断を、予測にもとづく判断に置き換える。予測に使うのは**世界の真の規則から作った結果の表**（例: 食べる → 体力がいくつ戻る）で、学習はしない。**各手を「窓の中で生き延びる確率の変化」で比べて選ぶ。** 子の通貨は、繁殖がまだ無いので常にゼロ。予測と値付けは1か所に実装し、その内訳をそのまま意思決定トレースとして出す。

**表は、1-6 で学ぶ粗い行と同じ形で持つ。** 1行 ＝（主語, 行動）→ 結果の分布（`NODE.md`「記憶の粒度の階段」）で、1-2 では中身を真の値で埋めるだけ。1-6 は行の読み先を経験に差し替えるだけで、形を変えない。1-2 に要る行は3種類。

| 主語（階段） | 行動 | 結果の分布 |
| --- | --- | --- |
| 食料のあるセル（地面） | 食べる | 体力 +`FoodEnergy`（上限まで） |
| 陸（地面） | 待つ／移動 | 体力 −`EnergyBurn`、位置が `Speed` だけ動く |
| この地域（地面） | 移動を続ける | 1タイル進むごとに、確率 p で**食料のあるセルに立つ**（p ＝ その地域の地面の食料 ÷ 陸のタイル数。1-2 ではその時点の真の値） |

- **3行目の結果の語（食料のあるセル）は、1行目の主語と同じ語にする。** 「移動 → 食料のあるセル」と「食料のあるセル → 体力」をつないで「移動 → 確率 p で体力が戻る」を組み立てる。`NODE.md`「記憶の合成」の最小の形で、中間項は消さない。
- **探索を目的の項にしない。** 「歩けば食料に出会うかもしれない」は、3行目の出会う確率として予測に入れる。次の食事までの時間が分布になるので、窓内の生存確率は 0/1 ではなく連続量になる。
- **最善の手が複数あれば、その中から無作為に選ぶ。** 差がつかない決定は 1-1（無作為）と同じ選び方になる。
- **予測は `engine/predict.go`（`TruthTable`・`NewSurvival`・`Value`）を使う。** 実装前の数え上げのために先に作ってあり、まだ `decide` にはつないでいない。生存確率の表は地域の地面の食料の数だけで決まるので、食料の数でキャッシュする（毎 tick 作り直すと、制約の地図で 1 tick 約 4 ms）。
- **知覚は入れない。** 隣のタイルの食料は見えない。同じ地域の中では8方向の移動が同点になり、差がつくのは地域の境目と、足元に食料があるときだけ。知覚は、1-2 の結果を見てから別の段階として足すかを決める（1段階1規則。視界の形はそれだけで世界を動かす）。

**実装前に数えること**
- **手の間に差がつく決定はどれだけあるか。** 1-1 の世界で決定の時点の状態を記録し、真の表を使って各手の窓内生存確率を**外で**計算する（世界には戻さない）。**「最善の手と2番目の手の差がゼロでない決定の割合」**を、窓の長さを変えながら数える。ほぼゼロなら、値付けを入れても世界は動かない（`CLAUDE.md` の教訓「まず非ゼロになる割合を数えるべき」）。
- **窓の長さはこの数え上げで決める。** 差がつく割合が寝始める長さを既定値にし、`PARAMETERS.md` に行を足す。
- **窓の締切の壁。** 窓より長くもつ身体（満腹に近い身体）では、どの手も窓内の生存確率が 1 になり、差がつかない。上の数え上げを**体力の帯ごとに分けて**出し、満腹に近い帯で差がつく割合がゼロに張り付くかを見る。張り付くなら、その身体は足元の食料を見送る。それで世界が困るか（食べる頻度が釣り合いに届くか）は、下の見積もりで読む。**壁を消す規則（窓を2段にする、など）は、この段階では入れない。**
- **足元の食料を確実に食べるだけで、釣り合いに届くか。** 1-1 の世界で、食料のあるタイルに立った回数を数え、「そのとき必ず食べた」とした場合の1体・1 tick あたりの食べる頻度を計算する。釣り合いの頻度（`EnergyBurn` ÷ `FoodEnergy`）に届かなければ、知覚の無い値付けでは全滅する。そうなったら、1-2 は作らずに記録し、知覚を先に入れるかを決め直す。
- **決定1回あたりの時間**を記録する（`PARAMETERS.md` の基準値を確定させるのは 1-6）。毎 tick、全個体が全部の手を先読みする最初の段階なので。
- **実験の長さを仮に決める**（1-1 では、全滅したため決められなかった）。1-2 は、個体数が横ばいになりうる最初の世界。
  - **tick 数**: 両方の地図で、個体数が横ばいになるまでの tick 数を数え、その数倍にする。
  - **シード数**: 横ばいの区間の個体数の、シード間の標準偏差を数える。1-3 以降の対の差を標準誤差の2倍で見分けられる本数にする。

**予想**
- **両方の地図で全滅しない。** 個体数は、初期の 200 から減って横ばいになる（出生が無いので増えはしない）。
- 1-1（無作為）との対の差で、**餓死数が両方の地図で有意に減る**。1体あたり半分以下になると予想する。1-1 は全員が餓死するので、この差は出て当然。本当に読むのは、上の「横ばいになるか」。
- **制約**: 地域別人口比が、地図の面積比から離れて**食料比のほうへ寄る**（選別）。無作為の実測は基準線にならない（1-1 は後半に生き残りがいない）ので、面積比と直接比べる。
- **同じ符号が両方の地図で出る。** 逆符号になったら、それ自体を記録に残す。
- §5 では「食べる」の割合が 1-1 より上がり、「待つ」が下がる。

**完了条件**
- 両方の地図で全滅せず、横ばいの区間がある。
- 両方の地図で、1-1 との対の差（餓死数）が有意。
- `cmd/experiment` に、シード数と tick 数の仮の既定値が入る。
- 意思決定トレースに出る数値と、実際の選択に使った数値が一致する（同じ関数から出ているので、テストで確かめる）。
- §5 で、どの手も一度は選ばれている（発火していない手が無い）。

**壊してはいけない線**
- 記憶（ここでは表）は結果だけを持ち、値打ちを持たない。表に書くのは「体力がいくつ戻る」までで、「だから +46」は毎回計算する（`NODE.md`）。
- 目的は2つだけ。時間は目的ではなくコスト（`NODE.md`）。
- **ハードコードされた行動閾値を作らない。** 「体力がこれ以下なら食べる」を書かない（`NODE.md`）。
- 予測の唯一の実装（`NODE.md`）。
- **目的を3つ目にしない**（`NODE.md`）。探索の値打ちを定数の項として足さない。

**判断に使う表**: §4、§5 行動の選択割合、§8、§9 対の差（base＝1-1）。

---

### 1-3 繁殖（子の通貨が働く・身体は全員同じ）

**何を作るか**
交配と出生を作る。子の身体は全員同じで、予算はまだ何も買わない。子は成人するまで交配を行えない（行動の可否として表す）。これで2つ目の通貨「子ができる確率の変化」が非ゼロになる。

**実装前に数えること**
- **子の通貨が非ゼロになる決定の割合。** 1-2 の世界で、交配できる相手が近くにいる決定の時点を数える。ほぼゼロなら、交配は起きず、世代も回らない。
- **世代は回るか。** 1-2 の世界で寿命の分布を数える。**老いによる死は設計文書に無いので入れない。** 死ぬ道が餓死だけのとき、満腹の大人がいつまでも生きるかどうかを先に見る。いつまでも生きるなら、それは 1-5（選択）と段階2（文化が個体より長生きするか）の前提を崩す発見として記録し、次の段階の「実装前に数えること」に回す。
- **成人までの期間**（設定値）を、上の寿命の分布に対して数える。寿命より長ければ、成人到達率は測れない。
- **崩壊の下限を仮に決める**（出生で個体数が戻りうる最初の世界）。崩壊しなかった実行の個体数の谷の分布を数え、その最小よりも下に置く。

**予想**
- 両方の地図で、個体数は食料の上限に張り付く（1-2 より上がり、そこで止まる）。
- **成人到達率は、制約の地図のほうが平坦な地図より低い。**
- **身体は陸でつながった地域すべてに広がり、水で隔てられた地域には入らない。** 広がりを抑える規則はまだ無いので、これは予想どおりに起きるべき問題として測る（`NODE.md`「普通の身体は自分では広がらない」）。

**完了条件**
- 両方の地図で、交配・出生が起き（§4）、崩壊しない。
- §5 で、交配が選ばれていて、閾値ではなく比較の結果として選ばれていることがトレースで読める。
- 占有した地域の数を時系列で記録できる（用語集に「占有地域数」を足す）。
- `cmd/experiment` に、崩壊の下限の仮の既定値が入る。

**壊してはいけない線**
- 目的は生命 > 子孫（`NODE.md`）。子の通貨は生存の通貨と同じ比較の中で扱い、別枠の欲求にしない。
- ハードコードされた行動閾値を作らない。「満腹なら交配する」を書かない（`NODE.md`）。
- 普通の身体は自分では広がらない（`NODE.md`）。この段階では抑えずに**測るだけ**にする。抑える規則を一緒に入れると、何が効いたか分からなくなる。

**判断に使う表**: §3、§4、§5、§8、§9（base＝1-2）、成人到達率（`MEASURE.md`「安い代用」）。

---

### 1-4 予算の配分（速さ ⇔ もう1つの能力）と最小のクライアント

**何を作るか**
身体に予算を持たせ、**速さと、もう1つの能力**に配る。**どちらへの変換も逓減させる**（`PARAMETERS.md`「硬い上限ではなく逓減」）。子の配分は、まだ**固定の分布から引く**（継承しない）。あわせて **ebiten（ネイティブ）の最小のクライアント**に主人公1体の操作を足す。見るだけのクライアント（`cmd/client`。描画と再生）は、`MEASURE.md`「UI での再生」のために先に作ってある。

変換を2つ同時に入れるのは、買い先が1つだと全員が同じ配分になり、何も測れないから。ルールとしては「予算を配る」の1つと数える。**クライアントは世界のルールではないので、1段階1規則の数には入れない。** ただし、engine 側に足す「行動を差し替える口」は世界に触れるコードなので、使わないときに世界を1ビットも変えないことを完了条件で確かめる。

**実装前に数えること**
- **速さの相手を何にするか。** 候補は3つ。
  - (a) 体力の上限（飢えに耐える長さ）だけ
  - (b) 速さの燃費（速い身体ほど移動で体力を使う）だけ
  - (c) (a) と (b) の両方

  1-3 の世界で、**全員一律の設定値として**「速さ」「体力の上限」「燃費」をそれぞれ上下に振る。(c) では体力の上限と燃費を同時に振る。どの候補でも、成人到達率と生涯出生数の対の差の傾きを両方の地図で数える。**両方の地図で両側の傾きが非ゼロになる候補を選ぶ。** 片側がゼロなら取り合いは起きず、配分は端に張り付く。**(a)・(b) とも片側の傾きがほぼゼロで、(c) だけ非ゼロなら、「代価1つでは足りない」という結果として記録し、(c) を採る。** 3つとも片側の傾きがほぼゼロなら 1-4 は作らない。結果を記録し、身体の代価の設計に戻る。
- **画面横断時間の見積もり。** 遊ぶズームと1フレームあたりの tick 数の仮の値で、1-3 の身体（全員同じ速さ）が画面を横切るのに何秒かかるかを計算する。この数字が 1-4 の「中央値の身体」の出発点になる。

**予想**
- 固定の分布から引いた配分でも、**生涯出生数は配分によって有意に違う**（選択勾配がある）。**制約の地図では速さの側、平坦な地図では相手の側に勾配が寄る**と予想する。外れたら、逆符号の例として記録する。
- 個体数は 1-3 から動かない（食料で頭打ちのため）。

**完了条件**
- 両方の地図で、配分と生涯出生数の関係（選択勾配）を §9 と同じ対の形で出せる。
- クライアントで主人公1体を動かせる。主人公は他のノードと同じ行動の集合を使い、engine からは区別がつかない。
- 行動を差し替える口を使わない実行では、挙動の指紋テストの値が、口を足す前と一致する。予算配分を入れると指紋の値が変わるので、**口は予算配分より先に足し、その時点で 1-3 の指紋と一致することを確かめる。**
- `PARAMETERS.md` の「予算→速さの変換」に仮の値が入る。

**壊してはいけない線**
- 能力の役割を重ねない（`NODE.md`）。速さは移動にだけ効かせ、相手の能力は1つの役割だけを持つ。
- **engine は「プレイヤー」を知らない**（`CLAUDE.md`）。操作は、行動を差し替える口を engine の外から叩く形にする。
- engine は描画を知らない（`CLAUDE.md`）。クライアントが `engine` を import する向きだけ。
- 種族ごとの分岐をコードに書かない（`CLAUDE.md`「種族」）。

**判断に使う表**: §3、§4、§9（base＝1-3）、`PARAMETERS.md` の速度の表。

---

### 1-5 予算の継承と突然変異（速度の分布の確定）

**何を作るか**
子の配分を、固定の分布ではなく**親から継承し、突然変異を加える**形に置き換える。継承の総量は保存する。**この段階の終わりで `PARAMETERS.md` の速度の3行を確定させる。**

**実装前に数えること**
- **選択が働くだけの世代が、実験の長さの中にあるか。** 1-4 の世界で、1-2 で仮に決めた tick 数の間に何世代回るかを数える。あわせて、1-4 で見つけた選択勾配の大きさから、配分の平均が標準誤差の2倍動くまでに何世代かかるかを見積もる。足りなければ、tick 数を延ばす。**延ばせる最後の機会はここ**で、1-5 の終わりに確定させたあとは変えない。
- **予算の保存則の帳簿。** 世界全体の予算の合計が、出生・死亡以外で変わらないことを検算する仕掛けを、継承を入れる前に入れる。
- **継承の形**（親から何をどれだけ渡すか）は `NODE.md` に書かれていない。候補を並べ、上の帳簿が閉じるものだけを残し、その中で1世代あたりの分散の減り方が一番小さいもの（選択が止まりにくいもの）を選ぶ。

**予想**
- 配分の平均は、1-4 の選択勾配の向きへ動く。**制約の地図では速さの側、平坦な地図では相手の側へ。**
- 逓減があるので、**配分は端に張り付かない**（最速の身体の割合が、世代が進んでも上がり続けない）。
- **最速 ÷ 中央値は、世代とともに一度広がってから落ち着く。**
- 上を締めても（逓減を強めても）、個体数は動かない（食料で頭打ち）。**成人到達率は、制約の地図でだけ有意に下がる。**

**完了条件**
- 予算の保存則の検算が全 tick で合う。
- 両方の地図で、配分の分布の世代推移が落ち着く（直近の世代どうしで平均の差が有意でない）。
- `PARAMETERS.md` の速度の4つの「測ること」が埋まる。
  1. 遊ぶズームでの、最速・中央値・最遅の画面横断時間
  2. 最速 ÷ 中央値
  3. **中央値の身体をクライアントで実際に操作して快適か**（ユーザが確かめる）
  4. 上を締めたとき、個体数と成人到達率が動くか
- 速度の表の3行（画面横断時間・最速÷中央値・予算→速さの変換）を確定し、根拠として `docs/history/` の日付へのリンクを入れる。
- **シード数・tick 数・崩壊の下限を確定させ、`cmd/experiment` の既定値にする。** 以後の全段階でこの値を使う。
- **ここで 1-2〜1-3 の base の数値を、確定した速度と上の値で取り直す。** 1-1 は全滅する対照なので、取り直しても横ばいの値は出ない。挙動の指紋テストを、この世界の値に更新する。

**壊してはいけない線**
- 保存則。予算の継承総量（`CLAUDE.md`「崩してはいけない原則」）。
- **速さに天井を置かない。** 逓減で上を締める（`PARAMETERS.md`）。
- **プレイヤーの身体に速さを贈与しない**（`PARAMETERS.md`）。遅すぎたら直すのは分布のほう。
- ハードコードされた行動閾値を作らない（`NODE.md`）。天井は閾値の一種になる。

**判断に使う表**: §3、§4、§8、§9（base＝1-4）、成人到達率、`PARAMETERS.md` の速度の表。

---

### 1-6 経験から学ぶ粗い行（伝承なし）

**何を作るか**
1-2 から使ってきた**真の規則の表を捨て**、各個体が自分の経験から**粗い行**（主語が種別・状況・地面の行）を学ぶ形に置き換える。推定は「一番細かい行を、証拠の数に応じて親の行へ引き寄せる」。**個体を主語にした行・伝承・合成は入れない。** 記憶容量と理性は、まだ予算に入れない（全員同じ）。記憶は持ち主が死ぬと消える。

**実装前に数えること**
- **学ぶ余地は実在するか。** 1-5 の世界で、決定の時点に「真の表で選んだ手」と「何も知らない（事前分布だけの）状態で選んだ手」が食い違う割合を数える。ほぼゼロなら、学習は世界を動かさない。
- **1個体は一生で何行ぶんの証拠を集めるか。** 1-5 の世界で、1個体が一生のうちに出会う（主語, 行動）の組の数と、組ごとの観測数の分布を数える。**観測数が少ない組が多いほど、親の行へ引き寄せる階段が効く。**
- **自力で学ぶ遅れ。** 上の分布から、真の表に近い推定に届くまでの年齢を見積もる。これが段階2の「伝承が働く余地」の下敷きになる。

**予想**
- 真の表の世界（1-5）との対の差で、**成人到達率が両方の地図で有意に下がる**。大人の餓死より**子どもの餓死**が増える（学ぶ前に死ぬ）。
- **平坦な地図では差が小さく、制約の地図では大きい。** 平坦な地図では、学ぶべきことがほとんど無いため。
- 選ばれる手の割合（§5）は、若い個体では 1-1（無作為）に近く、年齢とともに 1-5 に近づく。

**完了条件**
- 両方の地図で崩壊しない。
- §6 記憶の表が埋まる（中間項はまだ無いので、その行は空欄）。
- 意思決定トレースに、使った行・その証拠の数・親の行へどれだけ寄せたかが出る。
- 自力で学ぶ遅れ（年齢）を、段階2の基準として `docs/history/` に記録する。

**壊してはいけない線**
- 記憶は結果だけを持ち、値打ちを持たない（`NODE.md`）。
- **相手の真の能力は隠しパラメータ。知覚に真値を載せない（型で保証する）**（`NODE.md`）。学ぶ側のコードから、真の規則の表へ届く道を型で断つ。
- 能力の役割を重ねない（`NODE.md`）。記憶容量・理性は、予算に入れる段階で1つずつ入れる。

**判断に使う表**: §5、§6 記憶、§8、§9（base＝1-5）、成人到達率。

---

## 段階2 粗い行の伝承と「文化は個体より長生きするか」

`CLAUDE.md` の「最初の順序」の2にあたる。**訓練ステージも暖機もこの問いに賭けているので、偽なら段階3へ進まない。**

### 2-0 行の寿命を測る計器（ルールは入れない）

**何を作るか**
粗い行ごとに「世界の誰かが持っている期間」と「それを直接観測した個体が全員死んだあとも残っている期間」を記録する計器。乱数を引かず、世界を変えない（指紋テストで確かめる）。用語集に「行の寿命」「直接観測者」「又聞きの保有者の割合」を足す。

**実装前に数えること**
- 1-6 の世界（伝承なし）で、この計器が**「直接観測者が全員死んだあとの行の寿命＝ほぼゼロ」**を返すこと。伝承が無い世界でこれがゼロでなければ、計器が壊れている。

**完了条件**
- 伝承なしの世界で、上の値がゼロに張り付く。
- 指紋テストの値が 1-6 から変わらない。

### 2-1 伝承（経路1本）

**何を作るか**
粗い行を個体から個体へ渡す経路を**1本だけ**入れる。個体の行は渡さない。どの経路かは、下の数え上げで決める。

**実装前に数えること**
1. **伝承が働く余地は実在するか。** 1-6 で記録した「自力で学ぶ遅れ」から、行ごとに「一生のうち、その行を知らずに過ごす割合」と「知らない間に払ったコスト」（知らない個体と知っている個体の餓死率・成人到達率の差）を数える。**知らずに過ごす割合がほぼゼロの行ばかりなら、伝承は何も運ばない。** そうなったら 2-1 は作らずに記録し、地図の側（学ぶのに高くつく事実が実在するか）を見直す。
2. **その余地は、どちらの地図にあるか。** 上を地図ごとに分けて数える。平坦な地図では余地がほぼ無く、制約の地図（地域ごとに乏しさが違う）に集中するはず。
3. **経路を決める。** 1-6 の世界で、**親子（生まれたとき）** と **近くにいる者どうし** のそれぞれについて、次の3つの積を数える。
   - 機会: 一生あたり、その経路で相手と接する回数
   - 新しさ: 接したとき、渡す側が持っていて受け取る側が持っていない行の割合
   - 無知のコスト: 上の 1 で数えた、その行を知らないことのコスト

   **積は地図ごとに別々に読み、2枚の地図を合計も平均もしない。** 読み方は次のとおり。
   - シードごとに2つの経路の積を出し、同じシードで「親子 − 近くの者」の差をとる。その平均 ± 標準誤差を地図ごとに出す。
   - **どちらの経路も積が有意に非ゼロにならない地図は、経路の選択に使わない**（伝承の余地が無い地図）。数字は記録に残す。平坦な地図はここに入るはず。
   - 残った地図で、差が有意なほうの経路を1本だけ入れる。
   - **2枚とも非ゼロで、どちらの積が大きいかが食い違ったら、制約の地図に従う。** 完了条件を判定するのが制約の地図だからで、食い違いは逆符号の例として `docs/history/` に記録する。
   - **差が有意でなければ、4 で数える「全員が同じ行を信じるようになる速さ」が遅いほうの経路を選ぶ。** 交換の動機を殺しにくい側を選ぶため。

   もう片方の経路は、2-1 の結果を見てから、別の段階として足すかどうかを決める。
4. **全員が同じ行を信じるようになる速さ。** 選んだ経路で、行の推定値の個体間の分散がどれだけ速く縮むかを見積もる。中間項が出てくる段階（段階3以降）で交換の動機を殺さないかどうかの下敷きになる（`CLAUDE.md` の教訓「伝承は取引を殺す」）。

**完了条件（「文化は個体より長生きする」が真であること）**
- 伝承のある世界で、**直接観測者が全員死んだあとの行の寿命が、個体の寿命の中央値を超える行**がある。少なくとも制約の地図で。
- その行が世界を動かしている。伝承なし（1-6）との対の差で、**成人到達率か餓死数が有意に動く。** 残っているのに何も変えない行は、文化ではなく飾りとして扱う。
- **支えが切れても続く。** 伝承を入れたまま、世界の中で最初にその行を観測した世代がいなくなるまで走らせ、そのあとも上の2つが保たれる（`CLAUDE.md` の教訓「支えは自分より長く続かない」）。
- **偽なら段階3へ進まない。** 結果を `docs/history/` に残し、訓練ステージと暖機の前提を設計から見直す。

**壊してはいけない線**
- 伝わるのは粗い行だけ。個体の行は伝わらない（`NODE.md`「伝承は粒度で決まる」）。
- 中間項の値打ちを全員で一様にしない（`NODE.md`「記憶の合成」）。この段階ではまだ中間項は無いが、経路の形がそれを許さないことを確かめておく。
- 信念や `Lore` のような独立した機構を作らない。伝承は記憶の行を渡すだけ（`CLAUDE.md`「参考にしないもの」）。

**判断に使う表**: §3、§6 記憶、§8、§9（base＝1-6）、成人到達率、2-0 で足した行の寿命。

---

## 段階3以降（見出しだけ）

段階1・2の結果を見てから、実装できる粒度に割る。ここに挙がっていない規則（合成・物とコイン・戦い・好意・記憶容量と理性の予算化）も、そのときに位置を決める。

### 訓練ステージ
種族ごとの専用ステージ（汎用食料）で集団を育てる。段階2で長生きした文化が、訓練の中で身体とスキルに落ちるかを見る。

### セーブ
身体（予算・配分）とスキルだけを保存し、記憶は載せない（`NODE.md`「トレーニングと本番」）。前身の `engine/save.go` の作法を使う。

### 巣
本番マップで、セーブから個体を出す地点。種族ごとに複数。主人公がコインで設置する巣は Goal の段で扱う。

### 暖機
本番マップをプレイ開始前に一定期間進める。期間の既定値（10年）は、段階2で測った行の寿命から見直す。

### 種族
獣・鳥・魚を、行動の可否と予算の配り方だけの違いとして足す。記憶容量の予算化はここで入る。

### 成長・老い・性別
赤ん坊・青年・老人の段階、寿命、老化による死、性別の決め方。段階1・2では「成人かどうか」の2値だけで足りている（1-3「子は成人するまで交配を行えない」）。連続的な成長曲線・老化・性別の仕組みは、種族が入る段階3以降で決める。
老化が「回復経路を必ず残す」（`NODE.md`）とどう両立するかを、着手前に「実装前に数えること」として先に立てる（例: 老化以外の消耗は回復できることを条件にする）。

### Goal と継承
地域ごとの Goal・定住の判定・主人公の死後の操作の切り替え・クリアとゲームオーバー。すべてゲーム側に置き、engine には血統タグだけを持たせる（`CLAUDE.md`「engine は『プレイヤー』も『ゲームオーバー』も知らない」）。
