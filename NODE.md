# NODE.md

**この文書は今そうなっているルールだけを書く。経緯・旧版・測定値は `docs/history/YYYYMMDD.md` にある。** 大きくなったら各節を `docs/node/` へ切り出し、ここは索引にする。

---

## 種族

全種族が同じノード構造・同じ意思決定エンジンを使う。**違いは2つだけ**で、種族ごとの分岐をコードのどこにも書かない。

1. **行動として可能なこと**（移動の可否であって、判断ではない）
2. **予算の配り方**（どの能力に厚く振られるか）

| 種族 | できること | 備考 |
| --- | --- | --- |
| 人間 | 歩く | 記憶容量が大きく、細かい記憶を持てる |
| 獣 | 段差を越える（高台へ登れる） | 記憶容量が小さい |
| 鳥 | 飛ぶ | **天井を持つ**（越えられる高さに上限。未指定なら越えられない側を既定とする） |
| 魚 | 水中を移動する | 陸には上がれない |

**「因果の推論ができない」はフラグにしない。** 記憶の粒度の階段（下記）を入れれば、記憶容量の予算が小さい身体は細かい記憶を買えず、粗い記憶しか持てない。結果として獣は「あの人間」を覚えられず「人間というもの」しか覚えられない——**切ったのではなく買えないだけ**であり、予算を多く振った個体が現れればそこに選択圧が立つ。

**食べられるものは本番マップで種族ごとに異なる。** 例: 獣は生肉を食べられるが、人間は焼かないと食べられない。**同じ資源が種族によって別の通貨になる**——これも分岐を書かず、行動の可否だけで出す。対象となる物の定義は `ITEMS.md`。

---

## 身体と行動

- **体力**を持つ。毎 tick 一定量を消耗する（動いても動かなくても同じ）。尽きると**餓死**する。
- **足元のタイルの食料を食べると体力が戻る**（上限まで）。これが回復経路。
- 1 tick に行動を1つ取る: **待つ／食べる**（足元に食料があるとき）／**8方向への移動**（行き先が陸のときだけ）／**交配**（相手を名指しする。下の「繁殖」）。水や地図の外へ出る手と、**他の身体が立っているタイルへ入る手**（`Collide`。自分のタイルの中で動く手は入れる）は、候補に出ない——判断ではなく行動の可否。すでに同じタイルにいる身体（生まれた子と親など）は、そのままいてよい。
- 生まれたときは体力が満タン。
- **身体ごとの能力**（速さ・体力の上限・1 tick の消耗）を持ち、移動・食べる・消耗と値付けは自分の能力で読む（速い身体は自分の速さで予測する）。**予算の配分**（`Allot`）: 最初の身体は、予算のうち速さへ回す割合 s を、½ ± `AllotSpread` の `AllotLevels` 段から等しい確率で引く。**子は両親のどちらか一方の段をそのまま継ぎ**（どちらかは無作為）、確率 `AllotMutation` で1段上か下へずれる（端では内側へ）。速さ ＝ 設定の値 ×（2s）^`AllotCurve`、体力の上限 ＝ 設定の値 ×（2(1−s)）^`AllotCurve`。`AllotCurve` が 1 未満なので、片方へ寄せるほど1段あたりに買える量が減る（逓減。天井は置かない）。半分ずつなら設定の値。燃費は配らない（設定の値）。予算の大きさはまだ全員 `Budget`（1）で、生きている身体の予算の合計は生まれた分から死んだ分を引いたものに等しい（帳簿で毎 tick 検算する）。
- **判断は値付けで選ぶ。** 可能な手それぞれについて「窓（`Window` tick）の終わりに死んでいる確率」と「その手で子ができる確率」を予測し、**点数 ＝ 死んでいる確率 − `ChildWorth` × 子ができる確率**が一番小さい手を取る。`ChildWorth` は 1 未満（生命 > 子孫: 確実な子は確実な命より安い）。
- **判断は出来事が起きたときだけ行う**（`Recheck`）。決めた手は**意図**として持つ——食べる／見えている食料へ歩く（選んだ一歩がその食料への歩きを 1 tick 縮めるとき）／向きを保って歩く。**待つと交配は意図にならない**（1 tick で決め直す）。それ以外の tick は意図どおりに1歩進むだけで、値付けをしない。決め直すのは次のどれかが起きた tick: (1) 足元の食料の有無が変わった (2) 見えているものが変わった——**見え方は身体から見た位置**（自分のタイルからのずれ）で持つので、食料が見えている間はタイルをまたぐたびに変わり、何も見えていなければ変わらない (3) 視界の中の成人の顔ぶれが変わった (4) 次の一歩が陸の外・水・別の地域に出る、または他の身体が立っているタイルに入る (5) 食料へ歩いているとき、次の一歩がもう歩きを 1 tick 縮めない (6) `Recheck` tick のあいだ決めていない。**きっかけは「考え直せ」であって、何をするかは値付けが決める。** `Recheck` が 0 なら毎 tick 決める。**engine の外から選ぶ口**（`Chooser`。人が身体を操作する）が名指しした身体は毎 tick 決め（きっかけ `outside`）、口が返した手が候補にあればそれを、無ければ値付けの手を取る。engine から操作している身体は区別できない。
- **同じ値の手が複数あれば、向きを保つ**（`KeepHeading`）。直前の移動と同じ向きの移動がその中にあればそれを取り、無ければ同じ値の手の中から無作為に選ぶ。**跳ね返しの規則は持たない**: 陸の外・水・他の身体のいるタイルへの移動は候補に出ないだけで、より乏しい地域へ入る移動は死ぬ確率で負けるだけ——どちらも、残りの手を普通に比べる。窓が 0 で向きを保たなければ、全部の手が同じ値になり無作為な選択になる（1-1 の対照）。
- **予測は、世界についての行を自分の記憶から、身体と知覚についての行を真の値から作る**（`Learn`。記憶は下の「記憶と意思決定」）。表の行は5つ。
  1. 食料のあるセルで食べる → 体力が戻る
  2. 待つ／移動 → 体力が減る
  3. この地域で移動を続ける → 1タイル進むごとに、**自分が推定したその地域の食料の割合**で食料のあるセルに立つ。その向きの先 `PathAhead` タイルのうち、`PathRecall` tick 以内に自分が通ったタイルは、**自分が推定した「通ったタイル」の割合**で読む（引き返しは、通ったタイルが乏しいと学んでいれば、生存の通貨で負ける）。その場にとどまる手（待つ・食べる・交配）は、今ひらいている移動のうち一番良い向きでそのあと動き続けると読む
  4. 見えている食料のあるセルへ歩く → 8方向の移動での最短の tick 数（オクタイル距離 ÷ `Speed`）のあとに、そのセルに立つ
  5. 視界の中の成人と交配する → **自分が推定した割合**で、体力が `BirthEnergy` の半分減って子が1体生まれ、そうでなければ待つのと同じ
- 手を1つ取ったあとの続きは、「3行目で歩き続ける」と「決めた時点で見えていた食料の1つへ4行目で歩いて食べ、その食料の地域で3行目を続ける」のうち、死ぬ確率が低いほうで読む。**他の個体に先に食べられることは、4行目に入っていない。**
- **知覚**: 身体は、自分のいるタイルを中心とする (2×`Sight`+1)² のタイルの食料と、そこに立っている成人の身体が見える。相手の体力は見えない。

### 繁殖

- **成人**は生まれて `MatureAge` tick たった身体（最初の身体は初めから成人）。成人だけが交配でき、交配の相手になれる——行動の可否であって判断ではない。
- **交配は、視界の中の成人を名指しする手。** 候補に出るのは、自分の払う分（`BirthEnergy` の半分）を払っても体力が残るときだけ。値付けでは、払ったあとの体力で死ぬ確率を読み、子ができる確率を 1 とする（合意すれば必ず生まれる。相手が応じるかどうかは表に入っていない——他の身体に先に食べられることが入っていないのと同じ）。
- **出生は両者の合意でだけ起きる。** 交配の手を実行したとき、相手の今の意図が自分との交配で、相手がまだ視界にいて、両方が払えるなら、両親が半分ずつ払い、子が1体その場に生まれる（体力 `BirthEnergy`）。そうでなければ何も起きない（その tick は待つのと同じ）。生まれたあとは、どちらも交配の意図を持たない。
- 子は親2体の ID を持つ。予算も遺伝もまだ無く、子の身体は全員同じ。

## 記憶と意思決定

### 学ぶ行（1-6）

記憶は持ち主が死ぬと消える。各行は「（主語, 行動）→ 結果が N 回のうち K 回」で、推定は自分の数えを、行の重みの分の擬似観測で親の行の推定へ引き寄せたもの（`(K + 重み × 親) / (N + 重み)`）。

| 行 | 主語の段 | 親 | 重み |
| --- | --- | --- | --- |
| 自分が通ったタイルに入る → 食料がある | 地面（自分が通ったタイル） | その地域 | `PathWeight` |
| この地域のタイルに入る → 食料がある | 地面（地域） | 陸（自分が入った全地域をまとめたもの） | `RegionWeight` |
| 陸 | 地面の根 | 生まれつきの見込み `PriorFood` | `PriorWeight` |
| 同じ種の成人に交配を申し出る → 子が生まれる | 種別 | 生まれつきの見込み `PriorChild` | `ChildWeight` |

**証拠は、新しいタイルへ移ったときに視界に入ってきたタイル**（まっすぐなら3つ、斜めなら5つ）で、1タイルごとに食料があったか。`PathRecall` tick 以内に自分が通ったタイルなら、地域と通ったタイルの両方の証拠になる。自分が踏んだタイルは証拠にしない——見えた食料へ歩いて踏むので、食料があるように偏り、歩きを除くと、見えた食料は踏む前に歩いて取るので、無いように偏る。視界に入ってくるタイルは、身体が何を選んでも偏らない。交配は、申し出の手を実行するたびに1つ、子が生まれたら両親それぞれに1つ。生存の表は推定の割合を `BeliefStep` の累乗に丸めて作り、身体どうしで共有する。**身体の生理と知覚（食べて戻る体力・1 tick の消耗・歩く tick 数・視界の食料）は学ばず、真の値を知っている。**

### 記憶は結果だけを持つ

記憶が持つのは「**何が起きたか**」（結果の分布）だけで、**値打ちは持たない**。その結果が今の自分にとっていくらかは、その場で計算する。

> 「肉を食べた → 体力が大きく戻った」は記憶に入る。
> 「だから肉は +46」は身体の状態次第なので、毎回計算する。

こう分けることで、二重計上が原理的に起きない。

### 値付けは2通貨だけ

すべての手は「**窓の中で生き延びる確率の変化**」と「**子ができる確率の変化**」のどちらかに落ちる。この2つ以外に目的を作らない。落ちないものは、新しい項ではなく**予測モデルへの入力**である。

**例外は1つだけ**——それ自体が欲しいもの（飾り物など、終端の欲しさ）。例外があることを明示し、増やさない。

### 記憶の粒度の階段

1行 = **（主語, 行動）→ 結果の分布**。主語に階段を持たせる。

| 階段 | 主語の例 |
| --- | --- |
| 個体 | #42 |
| 種別 | 人間 / 獣の種類 / 品の種類 |
| 状況 | 乏しい・混んでいる・傷ついている |
| 地面 | このセル / この地域 / 水・荒れ地という種類 |

**細かい行は少ない証拠しか持てず、粗い行は多くの証拠を持つ。** 推定は「一番細かい行を、証拠の数に応じて親の行へ引き寄せる」。相手がいれば個体の行が効き、初対面なら種別に落ち、水のような相手なら地面の種類の行が効く——**切り替えは自動で出る**。

### 伝承は粒度で決まる

- **粗い行**（水は溺れさせる／この種の獣は重い／乏しいときの喧嘩は高くつく）は**伝わる**。集団の知識。
- **個体の行**（#42 に削られた／#42 は好き）は**伝わらない**。本人の経験。

これにより「噂で嫌いにならない」が、例外ではなく階段の帰結として満たされる。

### 記憶の合成

粗い行は、結果としても前件としても書ける（同じ語彙を使う）ので、**A→B と B→C から A→C を合成できる。** 合成は仮説であり、実際の観測より弱く持ち、観測が優先して訂正する。長い因果の鎖を、経験していない個体にも一段の記憶として持たせるための機構。

**合成しても B（中間項）の記憶は消さない。** A→C だけが残ると、B 自体の値打ちが式から消え、B を拾う・作る・手放さない理由が無くなる。合成は A→C という近道を**追加する**もので、A→B・B→C の置き換えではない。

**中間項の値打ちは固定せず、下流の結果から積む。** B の値打ちは「B を得た・持っていたあと、良い結果（生存・子孫）がどれだけ・どれくらいの頻度で続いたか」の記録そのもので、設定値としては置かない。コインが良いイベント（食事・回復・交配など）の前件として何度も現れれば、その頻度と大きさに応じて B（コイン）の記憶が積み上がり、値打ちが自然に上がる——**個体ごとに、経験の分だけ**。ここが崩れて全員が同じ値打ちを一様に信じるようにすると、コインを持つ理由が全員で揃ってしまい、交換する動機が消える（伝承は粒度の粗い行だけに留める理由の一つ）。

### 能力の役割

1つの能力に2つの役割を持たせない。持たせると、選択圧がどちらから来たのか測れなくなる。

| 能力 | 役割 |
| --- | --- |
| 記憶容量 | 何行持てるか／どの粒度まで細かい行を持てるか／どれだけ保つか |
| 理性 | 観測を取り込むときの精度 |
| 頭の良さ | 並べ替えの誤差 |

### 予測の唯一の実装

**予測と値付けの実装は1か所にまとめ、その内訳をそのまま画面（意思決定トレース）に出す。** こうすれば、トレースと実際の計算がズレようがない。

---

## トレーニングと本番

- **各種族は専用のトレーニングステージで育てる。** そこには汎用食料を置く。
- **育った結果をセーブし、本番マップの巣がそのセーブから個体を出す。**
- **セーブに載せるのは、身体（予算・配分）とスキルだけ。記憶は載せない。**

セーブが記憶を運ばない以上、巣から出た民は世界について何も知らない。本番マップ側の暖機（世界を何年か進めてから開始する仕組み）については `MAP.md` を参照。

---

## 崩してはいけない規則

- **記憶は結果だけを持ち、値打ちを持たない。** 値付けは2通貨でその都度行う。
- **目的は2つだけ**（生命 > 子孫）。時間は目的ではなくコスト。3つ目の目的を作らない（お金も、好意も、情報も、手段である）。
- **ハードコードされた行動閾値を作らない。** 「体力がこれ以下なら逃げる」のような条件は書かない。逃走も追撃も先制攻撃も売買も、比較の結果として現れさせる。再判断のトリガーは「考え直せ」であって「こうしろ」ではない。
- **能力の役割を重ねない。**
- **相手の真の能力は隠しパラメータ。** 知覚に真値を載せない（型で保証する）。
- **回復経路を必ず残す。** 「満腹かつ無理をしなければ回復する」が無いと、全員が単調に減って餓死するだけの世界になる。
- **普通の身体は自分では広がらない。** 放っておけば集団は世界じゅうに散り、Goal が勝手に埋まってゲームが成立しなくなる。抑え方（離れることへの代価など）は `docs/node/` に決まり次第記す——代価をかけすぎると貧しい土地の血統が留まって餓死する、という取引があることだけ先に書いておく。