# NODE.md

**この文書は今そうなっているルールだけを書く。経緯・旧版・測定値は `docs/history/YYYYMMDD.md` にある。** 大きくなったら各節を `docs/node/` へ切り出し、ここは索引にする。

---

## 種族

全種族が同じノード構造・同じ意思決定エンジンを使う。**違いは2つだけ**で、種族ごとの分岐をコードのどこにも書かない。

1. **行動として可能なこと**（移動の可否であって、判断ではない）
2. **予算の配り方**（どの能力に厚く振られるか）

| 種族 | できること | 備考 |
| --- | --- | --- |
| 人間 | 歩く | 記憶容量が大きく、細かい記憶を持てる |
| 獣 | 段差を越える（高台へ登れる） | 記憶容量が小さい |
| 鳥 | 飛ぶ | **天井を持つ**（越えられる高さに上限。未指定なら越えられない側を既定とする） |
| 魚 | 水中を移動する | 陸には上がれない |

**「因果の推論ができない」はフラグにしない。** 記憶の粒度の階段（下記）を入れれば、記憶容量の予算が小さい身体は細かい記憶を買えず、粗い記憶しか持てない。結果として獣は「あの人間」を覚えられず「人間というもの」しか覚えられない——**切ったのではなく買えないだけ**であり、予算を多く振った個体が現れればそこに選択圧が立つ。

**食べられるものは本番マップで種族ごとに異なる。** 例: 獣は生肉を食べられるが、人間は焼かないと食べられない。**同じ資源が種族によって別の通貨になる**——これも分岐を書かず、行動の可否だけで出す。対象となる物の定義は `ITEMS.md`。

---

## 身体と行動

- **体力**を持つ。毎 tick 一定量を消耗する（動いても動かなくても同じ）。尽きると**餓死**する。
- **足元のタイルの食料を食べると体力が戻る**（上限まで）。これが回復経路。
- 1 tick に行動を1つ取る: **待つ／食べる**（足元に食料があるとき）／**8方向への移動**（行き先が陸のときだけ）。水や地図の外へ出る手は、候補に出ない——判断ではなく行動の可否。
- 生まれたときは体力が満タン。
- **今の判断は、可能な手からの無作為な選択。** 下の値付けはまだ実装されていない。

## 記憶と意思決定

### 記憶は結果だけを持つ

記憶が持つのは「**何が起きたか**」（結果の分布）だけで、**値打ちは持たない**。その結果が今の自分にとっていくらかは、その場で計算する。

> 「肉を食べた → 体力が大きく戻った」は記憶に入る。
> 「だから肉は +46」は身体の状態次第なので、毎回計算する。

こう分けることで、二重計上が原理的に起きない。

### 値付けは2通貨だけ

すべての手は「**窓の中で生き延びる確率の変化**」と「**子ができる確率の変化**」のどちらかに落ちる。この2つ以外に目的を作らない。落ちないものは、新しい項ではなく**予測モデルへの入力**である。

**例外は1つだけ**——それ自体が欲しいもの（飾り物など、終端の欲しさ）。例外があることを明示し、増やさない。

### 記憶の粒度の階段

1行 = **（主語, 行動）→ 結果の分布**。主語に階段を持たせる。

| 階段 | 主語の例 |
| --- | --- |
| 個体 | #42 |
| 種別 | 人間 / 獣の種類 / 品の種類 |
| 状況 | 乏しい・混んでいる・傷ついている |
| 地面 | このセル / この地域 / 水・荒れ地という種類 |

**細かい行は少ない証拠しか持てず、粗い行は多くの証拠を持つ。** 推定は「一番細かい行を、証拠の数に応じて親の行へ引き寄せる」。相手がいれば個体の行が効き、初対面なら種別に落ち、水のような相手なら地面の種類の行が効く——**切り替えは自動で出る**。

### 伝承は粒度で決まる

- **粗い行**（水は溺れさせる／この種の獣は重い／乏しいときの喧嘩は高くつく）は**伝わる**。集団の知識。
- **個体の行**（#42 に削られた／#42 は好き）は**伝わらない**。本人の経験。

これにより「噂で嫌いにならない」が、例外ではなく階段の帰結として満たされる。

### 記憶の合成

粗い行は、結果としても前件としても書ける（同じ語彙を使う）ので、**A→B と B→C から A→C を合成できる。** 合成は仮説であり、実際の観測より弱く持ち、観測が優先して訂正する。長い因果の鎖を、経験していない個体にも一段の記憶として持たせるための機構。

**合成しても B（中間項）の記憶は消さない。** A→C だけが残ると、B 自体の値打ちが式から消え、B を拾う・作る・手放さない理由が無くなる。合成は A→C という近道を**追加する**もので、A→B・B→C の置き換えではない。

**中間項の値打ちは固定せず、下流の結果から積む。** B の値打ちは「B を得た・持っていたあと、良い結果（生存・子孫）がどれだけ・どれくらいの頻度で続いたか」の記録そのもので、設定値としては置かない。コインが良いイベント（食事・回復・交配など）の前件として何度も現れれば、その頻度と大きさに応じて B（コイン）の記憶が積み上がり、値打ちが自然に上がる——**個体ごとに、経験の分だけ**。ここが崩れて全員が同じ値打ちを一様に信じるようにすると、コインを持つ理由が全員で揃ってしまい、交換する動機が消える（伝承は粒度の粗い行だけに留める理由の一つ）。

### 能力の役割

1つの能力に2つの役割を持たせない。持たせると、選択圧がどちらから来たのか測れなくなる。

| 能力 | 役割 |
| --- | --- |
| 記憶容量 | 何行持てるか／どの粒度まで細かい行を持てるか／どれだけ保つか |
| 理性 | 観測を取り込むときの精度 |
| 頭の良さ | 並べ替えの誤差 |

### 予測の唯一の実装

**予測と値付けの実装は1か所にまとめ、その内訳をそのまま画面（意思決定トレース）に出す。** こうすれば、トレースと実際の計算がズレようがない。

---

## トレーニングと本番

- **各種族は専用のトレーニングステージで育てる。** そこには汎用食料を置く。
- **育った結果をセーブし、本番マップの巣がそのセーブから個体を出す。**
- **セーブに載せるのは、身体（予算・配分）とスキルだけ。記憶は載せない。**

セーブが記憶を運ばない以上、巣から出た民は世界について何も知らない。本番マップ側の暖機（世界を何年か進めてから開始する仕組み）については `MAP.md` を参照。

---

## 崩してはいけない規則

- **記憶は結果だけを持ち、値打ちを持たない。** 値付けは2通貨でその都度行う。
- **目的は2つだけ**（生命 > 子孫）。時間は目的ではなくコスト。3つ目の目的を作らない（お金も、好意も、情報も、手段である）。
- **ハードコードされた行動閾値を作らない。** 「体力がこれ以下なら逃げる」のような条件は書かない。逃走も追撃も先制攻撃も売買も、比較の結果として現れさせる。再判断のトリガーは「考え直せ」であって「こうしろ」ではない。
- **能力の役割を重ねない。**
- **相手の真の能力は隠しパラメータ。** 知覚に真値を載せない（型で保証する）。
- **回復経路を必ず残す。** 「満腹かつ無理をしなければ回復する」が無いと、全員が単調に減って餓死するだけの世界になる。
- **普通の身体は自分では広がらない。** 放っておけば集団は世界じゅうに散り、Goal が勝手に埋まってゲームが成立しなくなる。抑え方（離れることへの代価など）は `docs/node/` に決まり次第記す——代価をかけすぎると貧しい土地の血統が留まって餓死する、という取引があることだけ先に書いておく。