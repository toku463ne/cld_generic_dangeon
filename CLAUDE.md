# CLAUDE.md

## この文書について（メンテナンス方針）

- **ここには「今も有効な結論」だけを書く。** 経緯・理由・測定値は `docs/history/` に日付つきで残す。同じ議論を繰り返さないため。
- **追記は1項目1〜3行にとどめ、数値は書かない。** 「この規則は何を測ったか」をここに書き始めたら、行き先を間違えている。
- **上限 40,000 文字。** 超えそうになったら分割し、ここから参照する。前身プロジェクトはこの節が無かったために 150,000 文字の読み込み上限に達した。
- **規則を足すときは、どの既存の規則を畳むかを同時に言う。** 前身プロジェクトは「測って既定オフ」の規則が 300 以上堆積した。足すだけの運用は必ず同じ結果になる。
- 迷ったら「小さく追記」を優先する。既存の文章を大きく書き直す必要がある変更は、それ自体が設計判断として `docs/history/` に残すべきものである可能性が高い。

---

## プロジェクト概要

生物の振る舞い・適応・相対的能力・進化・賢さをテーマにしたゲーム。

プレイヤーは主人公（1ノード）を操作し、**世界マップの各地域にある Goal に自分の子孫を定住させること**を目指す。主人公が死んだら、各地域にいる子に操作を切り替えて続ける。何世代かにわたってクリアする。

人間以外にも複数の種族が存在し、植物も存在する。各種族は交配と突然変異により進化する。種族によって食べられるものが異なり、飛ぶ・泳ぐなどの移動能力も異なる。

ノードは経験と伝聞から記憶を持ち、その記憶による先読みを頼りに次の行動を決める。世界にはコインがあり、コインと引き換えに物を交換できる。各種族は巣から湧き出る。**主人公は Goal に子孫を残すため、コインを払って巣（村）を設置する。**

### ゲームの要素

- Goal を探索する
- 外敵と闘い、周囲の好意を得る
- 得た好意で仲間を集め、より強い外敵と闘う
- 交配相手を探し、好意を得て子孫を残す
- コインを集める
- スキルを得る

### 勝敗

- **クリア**: 指定された全ての Goal 地域に、自分の血統が定住していること
- **ゲームオーバー**: すべての地域で継承ラインが途絶えたとき

---

## 定義

- **ノード**: 個体。人間・獣・鳥・魚のいずれか。**全種族が同じ構造・同じ意思決定エンジンを使う。**
- **地域**: マップ上の区画。定住・Goal の単位。粗い記憶（伝承される知識）の主語にもなる。
- **巣**: ノードが世界に入ってくる地点。種族ごとに持つ。主人公はコインで設置できる。

---

## マップ

タイルで構成する。地形・地域・気温・食料・巣の詳細は `MAP.md` を参照。**地形（移動が何を要求するか）と地域（世界が何をくれるか）は別の地図**で、片方を塗っても他方は変わらない——これだけは横断的な設計方針として、ここにも残す。

作成は Tiled Map Editor（`.tmj` / JSON）。**engine はファイルを知らない**——読むのはクライアント側。

---

## 種族

全種族が同じノード構造・同じ意思決定エンジンを使う。違いは「行動として可能なこと」と「予算の配り方」だけで、種族ごとの分岐をコードのどこにも書かない。一覧と詳細は `NODE.md` を参照。

---

## トレーニングと本番

各種族は専用ステージで育て、身体とスキルだけをセーブして巣から本番マップへ送る。本番マップはプレイ開始前に一定期間（既定10年・要測定）暖機する。詳細は `NODE.md` を参照。

---

## 崩してはいけない原則

**ここに書くのは、複数の文書・複数の領域にまたがる原則だけ。** ノードの意思決定に固有の規則は `NODE.md`、物と交換に固有の規則は `ITEMS.md`、地形・地図に固有の規則は `MAP.md` の「崩してはいけない規則」に書く。

- **保存則を保つ。** 予算の継承総量（NODE）・食料と資源の総量（MAP）・物の総量（ITEMS）など、閉じているべき量を緩めるのは地図や種族の設計判断であって、既定ではない。
- **engine は描画も通信も知らない。**
- **engine は「プレイヤー」も「ゲームオーバー」も知らない。** クリア条件・継承・転移はゲーム側に置く。engine が持つとしたら、操作者と無関係な血統タグのような状態だけ。
- **決定性**——同じ設定・同じ種なら同じ結果。乱数は `World` が持つ1本に統一し、保存は「種と引いた回数」で行う。

---

## アーキテクチャ

- **言語: Go**（速度、既存の実績、ビルド・テストループの速さ）
- **構成: ブラウザで動く単体のクライアント。** ebiten を WASM 出力し、`engine` を直接 import する。サーバは当面作らない。
- **世界はプレイ中しか進まない。** 開発環境で育てたスナップショットを配り、その続きから始める。
- **後から足すとしても「セーブのアップロード」程度の薄い API** で、tick ごとの配信ではない。
- **決定性はプラットフォームをまたがない**（ネイティブと WASM で `math.Exp` / `math.Pow` が 1 ULP 違う）。**これは許容する**——この世界はプレイヤーが動かすので、続きはどのみちプレイヤー次第で変わる。**ブラウザどうしは同じ `.wasm` なら一致する。**

---

## 文書の分担

同じことを2箇所に書かない（必ず片方が古くなる）。

| 文書 | 役割 | 更新のしかた |
| --- | --- | --- |
| `CLAUDE.md` | 設計と規約の現在形。**複数文書にまたがる原則・アーキテクチャ・現在地** | 該当節を書き換える |
| `NODE.md` | **ノードが従うルールの概要。全種族共通**（種族ごとの文書は作らない） | **最終結果のみ。上書きする** |
| `ITEMS.md` | **物と交換の定義の概要。** コイン、スキル本、防寒着、保存食など、売買・交換の対象になるものの一覧 | **最終結果のみ。上書きする** |
| `MAP.md` | **マップの定義の概要。** タイル（地形・気温・地域）のパラメータ、食料ごとの保存則と湧く速度、巣の位置づけ | **最終結果のみ。上書きする** |
| `PARAMETERS.md` | パラメータ一覧と既定値。**冒頭に「最初に決めるパラメータ」**（他の測定より先に確定させるもの） | 上書きする |
| `MEASURE.md` | **測定レポートの形式・指標の定義・用語集。** 毎回この形で報告する | 指標を足したら用語集に1行足す |
| `README.md` | **今の操作方法だけ。** ビルド・テスト・起動オプション・画面の読み方 | 上書きする（経緯は書かない） |
| `PLAN.md` | これから作るものの設計・実装前に数えること・予想・完了条件 | 済んだ節は `docs/history/` へ移す |
| `TODO.md` | **まだ終わっていない作業だけ**を実装順に並べたもの | 済んだ項目は消す |
| `docs/history/YYYYMMDD.md` | 設計・実装ログ。**何をしたか・なぜそう決めたか。** 測定結果そのものは置かず、`docs/experiments/` へリンクする | 末尾に追記。過去は書き換えない |
| `docs/experiments/YYYYMMDD-段階名.md` | **`MEASURE.md` の形式で埋めた生のレポート。** 設計判断の理由づけとは分けて置く | 実験1回につき1ファイル追加 |
| `docs/node/`・`docs/items/`・`docs/map/` | **各概要文書が大きくなったときの詳細の置き場。** `NODE.md` / `ITEMS.md` / `MAP.md` はここへのリンクだけを持つ索引になる | 概要側の該当行と同じコミットで更新する |

**前身プロジェクトとの違い**: `ENEMY.md` は作らない（全種族が同じルールに従うため）。`docs/features/` に相当する分割先を `docs/node/` `docs/items/` `docs/map/` の3系統として最初から用意する（前身は肥大してから初めて分割した）。

### `NODE.md` / `ITEMS.md` / `MAP.md` は最終結果のみを持つ

この3つは**今そうなっているルールと定義だけ**を書く。経緯・旧版・検討過程・測定値は置かない。ルールが変わったら**古い記述を残さず上書きし**、消えた内容は `docs/history/YYYYMMDD.md` に「いつ・何を・なぜ」として移す。

- 「以前は〜だった」「〜は廃止した」はこの3つに書かない——`docs/history/` の仕事。
- 変更のたびに、その日の `docs/history/YYYYMMDD.md` に1項目足してから該当文書を上書きする。**順序はどちらが先でもよいが、同じコミットに入れる。**
- 読むときの前提が固定できる——**この3つは常に「今の姿」であり、日付をまたいだ差分を探す必要がない。**

---

## 測定

**測定レポートの形式・指標の定義・用語集は `MEASURE.md`。** 毎回同じ形式・同じ表で報告する。**新しい指標を足したら `MEASURE.md` の用語集に1行足すまでがセット**で、用語集に無い語をレポートに書かない。

測定の結果そのものは `docs/experiments/YYYYMMDD-段階名.md` に置く。`docs/history/` には、その結果から何を判断したかだけを書き、リンクする。

---

## 前身プロジェクトについて

このプロジェクトは https://github.com/toku463ne/cld_human_behaviour をベースにしている。**「ベース」とは、設計判断ログと一部の実装部品を指す。効用式の構造や測定値そのものを移植することではない。**

### 参考にしてよいもの

- `docs/history/` の測定値・予想が外れた記録・設計判断の理由 —— **罠が同じ形で再来しないための資料としてのみ**読む
- `engine/tiled.go`・`engine/save.go`・空間インデックス・保存/読込の決定性の作法（乱数1本、種と引いた回数）—— **実装をそのまま移植してよい部品**
- Tiled 連携、絵とスプライトの道具一式
- `cmd/experiment` の対の差の取り方・崩壊率の読み方 —— **測定の作法として踏襲する**

### 参考にしないもの

- **`Utility.Total()` とその17項の構造。** 記憶が値打ちを持つ設計そのものが、今回捨てた前提。項の名前や役割分担を新エンジンに写さない。
- **測定値そのもの。** `HISTORY.md` / `docs/stages.md` の数字は別の世界のもの。「前は○○だった」を新エンジンの既定値の根拠にしない。
- **`Config` のフィールド構成。** 300超のフィールドは「測って既定オフ」の堆積であって、仕様の一覧ではない。真似ると同じ速さで太る。
- **`ENEMY.md` という分離。** 新エンジンは全種族が同じルールに従うので、種族ごとの文書は作らない。
- **信念（`belief` / `Lore`）という独立した機構。** 新エンジンでは記憶の粒度の階段に吸収されるので、別枠として復元しない。

---

## 前身プロジェクトからの教訓

`cld_human_behaviour`（参照用に残す）で 100 段階以上を測って出た一般則。**数値は別の世界のものなので引き継がないが、罠は同じ形で再来する。**

- **情報を足しても居場所は動かない**（7度）。効用の掛け算側に入る情報は手の選び方を変えるが、足し算側に入る情報はまず「非ゼロになる割合」を数えるべき。
- **同じ規則が地図によって逆符号になる**（6例）。平坦な校正世界と、食料が制約になっている地図では結論が逆になる。**どちらで測ったかを必ず書く。**
- **社会的に伝わる仕組みは世界をほとんど動かさない。** 動いたのは構造的な制約（調理・地形・先読み）のほう。
- **伝承は取引を殺す。** 全員が同じ値打ちを信じると、交換する理由が消える。**貨幣を合意として立ち上げたいなら伝承が要るが、取引を起こしたいなら伝承は邪魔**——この2つは正反対の圧力である。
- **「死ではない消え方」を作った規則は、測定器の側でも死と区別すること。**
- **支えは自分より長く続かない。** 巣が支えた村は、支えが切れて100年たつと、その村が無かった世界と区別がつかなくなった。**新エンジンでは「文化が個体より長生きするか」を早い段階で測る**——真でなければ、訓練ステージも暖機も意味を失う。
- **1段階1規則で分解したとき、その規則が単独で世界を成立させられるとは限らない。** 前身は知覚・探索・値付けが一体で作られていたので、どれが効いているか分からない代わりに世界は動いていた。分解して1つずつ入れると、まだ足していない部品の不在が崩壊として露出する。**段階を切る前に、その規則が世界を成立させるために何を前提にしているかを、前身の実装から確認する**（`CLAUDE.md`「参考にしてよいもの」の設計判断ログ）。分解して初めて測れる数字が得られるのは利点だが、前提の確認を省くと手戻りになる。

---

## コーディング規約

- `go build ./... && go vet ./... && go test -race ./...` が常に通ること。
- 標準ライブラリで完結できるところは外部依存を増やさない。
- **シミュレーションの定数は package const ではなく設定のフィールドとして持つ。** テストでルールを1つずつ無効化して検証するため、そして A/B で条件を差し替えるため。設定が実質的な仕様の一覧にもなる。
- 新しいルールを追加したときは、**対応するテストと `NODE.md` の更新をセットで行う。** 物や交換に関わる変更は `ITEMS.md`、地形・地図に関わる変更は `MAP.md` も同じコミットで更新する。パラメータが増えたら `PARAMETERS.md` にも行を足す。
- **ホットパスに触れたときはベンチマークを変更の前後で取る。** 世界を進めるベンチは一定tickごとに世界を作り直す（作り直しの間はタイマーを止める）——1つの世界を回し続ける素朴な形は、一定の仕事量を測っていない。
- **1 tick あたりの時間と、決定1回あたりの時間に上限を置く。** 基準値と現在値は `PARAMETERS.md` に置く。超えたら、そのコミットの中で理由を `docs/history/YYYYMMDD.md` に書くか、遅くなった分を戻す。「動くから許容する」を無言でやらない。**上限は「変えるな」ではなく「気づかず変えるな」という制約**——正しい設計判断で超えるならそれでよく、そのときは基準値を更新して何を得て何を払ったかを残す。
- **記憶の粒度の階段は、記憶容量の予算がそのまま計算量の上限になる**（`NODE.md`）ので、決定1回あたりの時間は特にここで伸びやすい。
- **`cmd/experiment` の実行時間そのものも見る。** 測定を重くする変更（記憶の計器、合成の追跡など）は、測定の頻度を落とす動機になり、それ自体が測定の質を下げる。
- コードはすべて英語で書く。変数名・関数名・型名・コメント・ログ／エラーメッセージ・コミットメッセージを含む。**コード外のプロジェクト文書は日本語のままでよい。**

---

## コミットの方針

- **`main` に直接コミットする。ブランチは切らない。**
- コミットメッセージは英語で書く。
- **1つの機能・1つの測定 ＝ 1コミット。** 実装・テスト・文書の更新は同じコミットに入れる。
- 記録用の成果物（画像など）は、それを生成したコードとは別のコミットにする。順序は「コードを先にコミット → そのハッシュで成果物を生成 → 成果物を次のコミットに入れる」。
- 本文には「何を変えたか」だけでなく「**なぜその形にしたか**」の要点を書く。特に**採らなかった素直な選択肢とその理由**は残す価値が高い。
- **コミットするのはユーザから指示があったとき。** 指示なく勝手にコミットしない。

---

## 最初の順序

**訓練ステージも暖機も「文化が個体より長生きする」ことに賭けた仕組みなので、賭けの成否を先に確かめる。**

1. **最小構成**（身体・食料・水のみ。個体の記憶も伝承も無し）で、「記憶は結果、値付けは2通貨」が動くことを確かめる。**同じ段階で速度の分布を確定させる**（`PARAMETERS.md` の「最初に決めるパラメータ」——後から変えると先行する測定が全部読み直しになる）
2. **粗い行だけの伝承**を足し、**文化が個体より長生きするか**を測る
3. そこで初めて、訓練ステージ・セーブ・巣・暖機・種族・Goal へ進む