# MAP.md

**この文書は今そうなっているルールだけを書く。経緯・旧版・測定値は `docs/history/YYYYMMDD.md` にある。** 大きくなったら各節を `docs/map/` へ切り出し、ここは索引にする。

---

## タイルとレイヤー

タイルで構成する。タイルごとに地形・気温を定義し、各種パラメータを与える。

- **地形**は「移動が何を要求するか」（タイル単位）
- **地域**は「世界が何をくれるか」（区画単位）
- **この2つは別の地図**で、片方を塗っても他方は変わらない。

作成は Tiled Map Editor（`.tmj` / JSON）。**engine はファイルを知らない**

---

## 気温

地域（または個別セル）ごとに気温を定義する。詳細は `docs/map/` に決まり次第記す。

---

## 食料

指定されたタイルに自然と沸き、すべての種族が利用できる。  
- **食料ごとに保存則を持つ**（同時に存在する総量）。詳細は `docs/map/`
- **食料は地域ごとに湧く速度を持つ。** 総量は地図全体で保存され、地域ごとの濃淡は「取り分」であって新しく生む量ではない。
- 食料は1種類（汎用食料）。**陸のタイルに、1タイル1単位まで**置かれる。
- **上限 ＝ 地面にある量 ＋ 空き。** 食べられた単位は空きになり、空きは毎 tick 一定の確率で戻る。**戻る先の地域は、地域ごとの取り分の比で決まる**（地域の中では陸のタイルから無作為）。
- 世界は、食料が上限まで置かれた状態で始まる。
- 地域ごとの取り分は地図が持つ（地域レイヤーの属性）。上限と戻る確率は設定が持つ。

---

## 巣
- ノードが世界に入ってくる地点。**種族ごとに複数持つ。**
- 主人公はコインを払って巣（村）を設置できる。

