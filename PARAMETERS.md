# PARAMETERS.md

**この文書は今の値と、それをどう決めたかの根拠へのリンクだけを持つ。** 測定の経緯と数値は `docs/history/YYYYMMDD.md`、測定の形式は `MEASURE.md`。

---

## 最初に決めるパラメータ

**他の測定より先に決める。** これらは移動コスト・レース勝率・逃走成功率・出会いの頻度など、ほぼ全ての規則に入るので、後から変えると先行する測定が全部読み直しになる。**最小構成（身体・食料・水）の段階で確定させる。**

### 速度の分布

**目的**: プレイヤーが快適に操作でき、かつ他のノードがプレイヤーより極端に速くないこと。**遅い側に制限は要らない。**

**単位は世界単位の速度ではなく「画面を横切るのに何秒かかるか」。** ズーム倍率と1フレームあたりの tick 数が同じだけ効くので、遺伝子の値だけを見ても答えが出ない。

**縛るのは絶対値ではなく比**——プレイヤーの身体は集団から選ばれるので、効くのは「集団の最速 ÷ プレイヤーの身体の速さ」。片側だけの制約。

| 項目 | 値 | 根拠 |
| --- | --- | --- |
| 画面横断時間（中央値の身体） | **4.1〜4.2 秒**（×1。地図全体 64 タイル、60 フレーム/秒。確定 2026-09-26） | [1-5](docs/experiments/20260926-1-5.md)。中央値の身体をユーザが操作して快適だった（[20260926](docs/history/20260926.md)） |
| 最速 ÷ 中央値 | **1.12〜1.14**（確定 2026-09-26） | [1-5](docs/experiments/20260926-1-5.md)。上を締める（逓減 0.25）と 1.07〜1.08 になり、成人到達率がわずかに下がる |
| 予算→速さの変換 | **速さ ＝ 0.25 ×（2s）^0.5**、s は速さへの配分（½ ± 0.25 の 11 段、継承）（確定 2026-09-26） | 相手は体力の上限（[1-4 数え上げ](docs/experiments/20260926-1-4-invade.md)）。逓減を 0.25 に強めても個体数は動かず成人到達率がわずかに下がるので、0.5 のまま（[1-5](docs/experiments/20260926-1-5.md)） |

**測ること**

1. **画面横断時間の分布** — 遊ぶズームで、最速・中央値・最遅の身体が画面を横切るのに何秒か
2. **最速 ÷ 中央値** — 視覚的な違和感の指標そのもの
3. **中央値の身体が快適に操作できるか** — 実際に操作して確かめる（数字では出ない）
4. **上を締めたとき、個体数と成人到達率が動くか** — 動かなければ、この制約はただで買える

**形は「硬い上限」ではなく「予算→速さの変換を逓減させる」。** 天井を置くと個体がそこに張り付いて選択が止まり、閾値を作らない原則とも衝突する。逓減なら天井を書かずに上が締まる。

**主人公の速さの補正は下限だけ・予算の外・ゲーム側に置く**（`VISION.md`「遺伝子」）。遅くなりすぎないよう下から持ち上げるだけで、予算の配分は変えず、遺伝子として子へは渡らない（操作が子へ移れば、その子に同じ補正がかかる）。engine からは他の身体と区別がつかない形で入れる。**分布の設計の目標は変えない**——集団の中央値の身体が補正なしで快適に操作できるように作り、補正が効く（下限に当たる）主人公の割合を測る。補正が常に効くなら、直すのは分布のほう。

---

## 性能の上限

**CLAUDE.md「コーディング規約」の実体。** 目標は体感で決め、数字はそれを日々確かめるための代理指標として持つ。代理指標が悪化したら、理由を `docs/history/YYYYMMDD.md` に書くか戻す。

### 最終目標（体感）

**スマートフォンのブラウザ（WASM・1コア）で、本番マップの同時個体数の世界を、プレイ中にリアルタイムで計算しながら、止まらずに遊べること。** セーブ（配るスナップショット）を読み込んだ直後も含む。

- 「止まらない」: 普通の速さ（1フレーム 1 tick、60 フレーム/秒）で描画込みでフレームを落とさず、速い設定（1フレーム 8 tick。仮）でも追いつく。
- 1フレーム（16.7 ms）のうちエンジンに使ってよいのは半分（残りは描画。仮）。速い設定で 1 tick あたり約 1 ms（スマートフォン）。
- 本番マップの同時個体数は未定（本番マップを作る段階で決める）。それまでは今の世界の約 170 体で見る。
- **スマートフォンでは測っていない。** node（この開発機）の 2〜4 倍遅いと見る（前身の見積もり）。本番マップの段階で実機で確かめる。

### 代理指標

| 指標 | 何を代理するか | 今の値 | いつ見るか |
| --- | --- | --- | --- |
| `GOMAXPROCS=1 go run ./cmd/speed`（constrained・170 体・10000 tick の秒数） | 1コアでのエンジンの重さ | 1.02〜1.09 秒（102〜109 µs/tick。学ぶ行が入って約 2 倍。学ばない `truth` で 0.53 秒） | 規則を足した段階ごと |
| 同じものを WASM で node（`cmd/speed` の説明） | ブラウザでの重さ。スマートフォンは × 2〜4 | 1.7 秒（170 µs/tick）。速い設定で 1 フレーム約 1.4 ms ＝ スマートフォンで 2.7〜5.4 ms、目標の半フレーム（8.3 ms）に入る | 規則を足した段階ごと |
| 同じ測定の「読み込んで温めた（`World.Warm`）あとの 1000 tick」と温める時間 | セーブから始めた直後の重さと、読み込みの待ち時間 | 温めたあと ネイティブ 63 µs/tick・WASM 190 µs/tick（定常の 1.1〜1.3 倍）。温めるのに ネイティブ 0.25 秒・WASM 0.63 秒（スマートフォンで 1.3〜2.5 秒の読み込み。`Warm(n)` で数フレームに分けられる）。温めないと 1000 tick が定常の約 4.5 倍 | 同上 |
| 同じ測定の「保持している生存の表」とヒープ | スマートフォンのメモリ | 表 約 420、ヒープ 約 61 MB（表を捨てないと 20000 tick で約 2600・約 290 MB） | 同上 |
| `go test ./engine -bench 'WorldStep\|Decide'`（µs） | 上の3つの、コミットごとの早い目安 | `WorldStep` 約 220 µs（テスト用の小さな地図で 1000 tick ごとに作り直す＝表の作り直しを含む）、`Decide` 約 760 ns | ホットパスに触れたコミットごと。1桁 % 以上の悪化は理由を書く |
| `cmd/experiment` 1回（12 シード × 40000 tick）の時間 | 測定の頻度を落とさないこと | 1シード 4.6 秒 | 段階ごと |

根拠と前身との比較: [20260926-speed](docs/experiments/20260926-speed.md)（同じくらいの個体数・同じ tick 数で、前身より約 10 倍速い）。

### 前身の実績を目安に使えるか

前身（`cld_human_behaviour`）の「動く」の確認は次のとおりだった（前身の `PLAN.md`・`docs/history/decision/6.md`）。

| | 前身の確認 | 今回の想定 | 差 |
| --- | --- | --- | --- |
| 同時個体数 | 遊ぶ地図で人 約 120〜150 ＋ 外敵 約 10〜15（物・食料 約 130） | 本番マップ（未定）。今の世界は 約 170 | 近い。本番マップが大きくなれば比例して重くなる（空間の格子があるので、個体数にほぼ比例） |
| リアルタイムか再生か | **リアルタイム**（`cmd/devview` がプレイ中に `Step` を呼ぶ。1 フレーム 2 tick／8 tick で回した） | リアルタイム | 同じ |
| ネイティブか WASM か | **両方、ただし開発機だけ**（node で 614 µs/tick、Chrome headless で 11〜14 fps。スマートフォン実機は無し） | WASM・スマートフォン | 実機の分だけ不確か（× 2〜4 と見る） |

**目安として使えるが、今の値は前身より約 10 倍速いので、前身の実績は「下限」としては緩すぎる。** 今回の差分で悪化しうるのは2つ: (1) 記憶の階段（1-6 以降）で決定1回が重くなる——前身は決定1回 8〜37 µs、今は約 0.7 µs で、前身並みになれば 1 tick も前身並み（約 10 倍）になりうる。(2) 種族と本番マップで個体数が増える（比例）。読み込み直後の表の作り直しは直した（上の表）。(1) がいちばん大きく、決定の割合（1-2e の 6〜14%）を保つことが効く。

---

## 測定の既定値（世界のルールではない）

`cmd/experiment` の既定値。**1-5 で確定させた（2026-09-26）。以後の全段階で使う**（`PLAN.md` 1-5）。

| 項目 | 値 | 根拠 |
| --- | --- | --- |
| シード数 | **12**（確定） | 1-2r の 40000 tick の人口のシード間の標準偏差は、平坦な地図で約 1.4 体、制約の地図で約 12 体。12 本で制約の地図の平均の標準誤差が約 3.6 体になり、1-2q → 1-2r の対の差（+46 体）は十分に読める（[20260925](docs/history/20260925.md)） |
| tick 数 | **40000**（確定） | 1-2r で両方の地図が tick 10000 までに横ばいになり、その後 30000 tick を観察できる長さ（[20260925](docs/history/20260925.md)）。1-5 で約 37 世代が回り、配分の分布が tick 25000 までに落ち着く（[1-5](docs/experiments/20260926-1-5.md)） |
| 崩壊の下限 | **20**（確定） | 1-3 の崩壊しなかった実行の個体数の谷は最小 119（平坦 122・制約 119）。その 6 分の 1 に置き、健全な実行が近づかない高さにした。交配に要る成人 2 体よりは十分上で、戻りうる崩れかけを数えられる（[20260926](docs/history/20260926.md)） |

---

## パラメータ一覧

（決まり次第、領域ごとに表を足す。大きくなったら `docs/node/` `docs/items/` `docs/map/` の該当箇所へ切り出す。）

### 身体と食料（1-1。すべて仮）

| 項目 | フィールド | 値 | 根拠 |
| --- | --- | --- | --- |
| 食料の上限 | `FoodCap` | 300 | 仮。64×48 の地図で陸のおよそ1割 |
| 空きが戻る確率（/tick） | `FoodReturn` | 0.002 | 仮 |
| 1単位で戻る体力 | `FoodEnergy` | 30 | 仮 |
| 初期の個体数 | `Bodies` | 200 | 仮 |
| 体力の上限 | `EnergyMax` | 100 | 仮 |
| 体力の消耗（/tick） | `EnergyBurn` | 0.1 | 仮。満腹から餓死まで 1000 tick |
| 速さ（タイル/tick） | `Speed` | 0.25 | 仮。全員同じ。1-5 で確定 |
| 制約の地図の地域の取り分 | `worldmap.ConstrainedFood` | 0.5 / 0.1 / 0.3 / 0.1 | 仮 |

### 値付け（1-2。仮）

| 項目 | フィールド | 値 | 根拠 |
| --- | --- | --- | --- |
| 窓の長さ（tick） | `Window` | 1000 | 差がつく決定の割合が寝る長さ。満腹から餓死までの tick 数（`EnergyMax` ÷ `EnergyBurn`）と同じで、これより短いと満腹に近い身体が足元の食料を見送る。体力の値を変えたら数え直す（[20260924](docs/history/20260924.md)） |

### 知覚（1-2p。仮）

| 項目 | フィールド | 値 | 根拠 |
| --- | --- | --- | --- |
| 視界の半径（タイル） | `Sight` | 1 | 自分のタイルを中心とする 3×3。新しく食料が見える頻度が釣り合いを超える一番小さい半径（半径 0 は 0.72 倍、1 は約 7.6 倍）。体力・食料・速さの値を変えたら数え直す（[20260924](docs/history/20260924.md)） |

### 向きの持続（1-2q）

| 項目 | フィールド | 値 | 根拠 |
| --- | --- | --- | --- |
| 同点で直前の向きを保つ | `KeepHeading` | true | 視界に食料が無い決定では移動が同点になり、無作為の歩行では身体が自分の食べ尽くした範囲から出られない（[20260924](docs/history/20260924.md)）。数値のパラメータは持たない |
| 塞がれた向きを跳ね返す | `Bounce` | **false** | 1-2q〜1-5 は true（その軸の成分を反転する）。**ユーザの判断で外した**（2026-09-26）: 歩き方を良くするために足した作為の規則で、記憶による探索に置き換わるべきもの。外すと縁にいる身体が約 2 倍、個体数 −5〜−6（[bounce](docs/experiments/20260926-bounce.md)）——それを 1-6 以降の改善を測る基準にする。条件 `bounce` が 1-5 の世界 |
| 跳ね返しが真後ろのときだけ無作為に選ぶ | `DrawOffReverse` | false | `Bounce` のときだけ。比べるための条件（`drawreverse`） |
| 真後ろへ跳ね返さない（45°） | `TurnOffReverse` | true（`Bounce` のときだけ効く） | 軸に沿った向きは真後ろへ跳ね返り、同じ列か行を往復する。1-2q の世界で死亡の 27〜48% がこれで終わっていた（[20260925](docs/history/20260925.md)）。数値のパラメータは持たない |

### 判断のタイミング（1-2e。仮）

| 項目 | フィールド | 値 | 根拠 |
| --- | --- | --- | --- |
| 何も起きないときに決め直す間隔（tick） | `Recheck` | 300 | 1食で戻る体力を燃やす tick 数。1-2r の世界で同じ手が続いた長さは最大 271 tick なので、ほぼ安全網としてだけ働く。0 で毎 tick 決める（[20260926](docs/history/20260926.md)） |

### 繁殖（1-3。仮）

| 項目 | フィールド | 値 | 根拠 |
| --- | --- | --- | --- |
| 交配と出生がある | `Breed` | true | false で 1-2e の世界（条件 `nobreed`） |
| 確実な子の値打ち（確実な死に対して） | `ChildWorth` | 0.5 | 1 未満で「生命 > 子孫」。親子の近縁度。形はユーザが決めた（[20260926](docs/history/20260926.md)） |
| 子の生まれたときの体力（両親が半分ずつ払う） | `BirthEnergy` | 50 | 満腹の半分（500 tick）。食事の周期 300 tick より長い（[20260926](docs/history/20260926.md)） |
| 成人までの tick 数 | `MatureAge` | 1000 | 満腹から餓死までの tick 数。成人するには一度は自分で食べる必要がある（[20260926](docs/history/20260926.md)） |

### 予算の配分（1-4。仮）

| 項目 | フィールド | 値 | 根拠 |
| --- | --- | --- | --- |
| 予算を速さと体力の上限に配る | `Allot` | true | false で 1-3 の世界（条件 `fixed`） |
| 速さへの配分の幅（½ からのずれ） | `AllotSpread` | 0.25 | 仮。配分 0.25〜0.75 で、速さ ×0.71〜×1.22、上限 ×1.22〜×0.71。侵入の数え上げ（×0.8・×1.25）と同じくらいの幅 |
| 配分の段の数 | `AllotLevels` | 11 | 生存の表を身体どうしで共有するため（表1つが窓 × 満腹の tick 数の計算）。連続にすると身体ごとに表を作り、テストが 10 倍遅くなった（[20260926](docs/history/20260926.md)） |
| 変換の逓減 | `AllotCurve` | 0.5 | 仮。1 未満で逓減。半分ずつで設定の値 |
| 配分を親から継ぐ | `AllotInherit` | true | 両親のどちらか一方の段（1-5）。false で配分を毎回引く世界（条件 `drawn`） |
| 突然変異の確率 | `AllotMutation` | 0.1 | 仮。1段ずらす |
| 予算の大きさ | `Budget` | 1 | 全員同じ。帳簿のために持つ |

### 衝突（仮）

| 項目 | フィールド | 値 | 根拠 |
| --- | --- | --- | --- |
| 他の身体のいるタイルへ入れない | `Collide` | true | false で 1-4 の世界（条件 `overlap`）。数値のパラメータは持たない。形はユーザと決めた（[20260926](docs/history/20260926.md)） |

### 学ぶ行（1-6。仮）

| 項目 | フィールド | 値 | 根拠 |
| --- | --- | --- | --- |
| 世界についての行を記憶から読む | `Learn` | true | false で真の表の世界（条件 `truth`） |
| 生まれつきの見込み（食料 / タイル） | `PriorFood` | 0.01 | 仮。今の地図の平均（0.013）に近い。値を振っても決定はほぼ変わらない（[1-6-learn](docs/experiments/20260926-1-6-learn.md) の (A)） |
| 生まれつきの見込みの重み（タイル） | `PriorWeight` | 50 | 仮 |
| 地域 → 陸 の重み（タイル） | `RegionWeight` | 50 | 仮 |
| 通ったタイル → 地域 の重み（タイル） | `PathWeight` | 20 | 仮。一生で通ったタイルに入るのは約 45 回（[1-6-room](docs/experiments/20260926-1-6-room.md)） |
| 通ったタイルを覚えている tick 数 | `PathRecall` | 200 | 仮。数え上げと同じ |
| 向きの先を見るタイル数 | `PathAhead` | 8 | 仮。数え上げと同じ |
| 交配が子になる生まれつきの見込み | `PriorChild` | 1 | 真の表と同じところから始め、経験で下がる |
| その重み（申し出） | `ChildWeight` | 2 | 仮 |
| 推定を丸める比（生存の表の共有） | `BeliefStep` | 1.1 | 推定の割合を 1.1 の累乗に丸める。表の数を抑えるため |
